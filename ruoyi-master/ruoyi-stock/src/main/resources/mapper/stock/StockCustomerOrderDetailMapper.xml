<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.stock.mapper.StockCustomerOrderDetailMapper">

    <resultMap type="StockCustomerOrderDetail" id="StockCustomerOrderDetailResult">
        <result property="detailId"      column="detail_id"       />
        <result property="orderNo"       column="order_no"        />
        <result property="lineNo"        column="line_no"         />
        <result property="matCode"       column="mat_code"        />
        <result property="matName"       column="mat_name"        />
        <result property="spec"          column="spec"            />
        <result property="quantity"      column="quantity"        />
        <result property="deliveredQty"  column="delivered_qty"   />
        <result property="unitPrice"     column="unit_price"      />
        <result property="amount"        column="amount"          />
        <result property="prodOrderNo"   column="prod_order_no"   />
        <result property="unitCode"      column="unit_code"       />
        <result property="figNum"        column="fig_num"         />
        <result property="delFlag"       column="del_flag"        />
        <result property="createBy"      column="create_by"       />
        <result property="createTime"    column="create_time"     />
        <result property="updateBy"      column="update_by"       />
        <result property="updateTime"    column="update_time"     />
        <result property="remark"        column="remark"          />
    </resultMap>

    <sql id="selectDetailVo">
        select detail_id, order_no, line_no, mat_code, mat_name, spec, quantity, delivered_qty,
               unit_price, amount, prod_order_no, del_flag, create_by, create_time, update_by, update_time, remark
        from stock_customer_order_detail
    </sql>

    <select id="selectStockCustomerOrderDetailByDetailId" parameterType="Long" resultMap="StockCustomerOrderDetailResult">
        <include refid="selectDetailVo"/>
        where del_flag = '0' and detail_id = #{detailId}
    </select>

    <select id="selectDetailListByOrderNo" resultMap="StockCustomerOrderDetailResult">
        select d.detail_id, d.order_no, d.line_no, d.mat_code, d.mat_name, d.spec, d.quantity, d.delivered_qty,
               d.unit_price, d.amount, d.prod_order_no, d.del_flag, d.create_by, d.create_time, d.update_by, d.update_time, d.remark,
               m.unit_code, m.fig_num
        from stock_customer_order_detail d
        left join base_mat m on d.mat_code = m.mat_code COLLATE utf8mb4_general_ci and m.del_flag = '0'
        where d.del_flag = '0' and d.order_no = #{orderNo}
        order by d.line_no asc
    </select>

    <select id="selectStockCustomerOrderDetailList" parameterType="StockCustomerOrderDetail" resultMap="StockCustomerOrderDetailResult">
        <include refid="selectDetailVo"/>
        WHERE del_flag = '0'
        <if test="orderNo != null and orderNo != ''"> and order_no = #{orderNo}</if>
        <if test="matCode != null and matCode != ''"> and mat_code = #{matCode}</if>
        <if test="matName != null and matName != ''"> and mat_name like concat('%', #{matName}, '%')</if>
        ORDER BY line_no ASC
    </select>

    <insert id="insertStockCustomerOrderDetailList" parameterType="java.util.List">
        insert into stock_customer_order_detail (order_no, line_no, mat_code, mat_name, spec, quantity,
            delivered_qty, unit_price, amount, prod_order_no, del_flag, create_by, create_time)
        values
        <foreach item="item" collection="list" separator=",">
            (#{item.orderNo}, #{item.lineNo}, #{item.matCode}, #{item.matName}, #{item.spec}, #{item.quantity},
             #{item.deliveredQty}, #{item.unitPrice}, #{item.amount}, #{item.prodOrderNo}, '0', #{item.createBy}, #{item.createTime})
        </foreach>
    </insert>

    <update id="updateStockCustomerOrderDetail" parameterType="StockCustomerOrderDetail">
        update stock_customer_order_detail
        <trim prefix="SET" suffixOverrides=",">
            <if test="lineNo != null">line_no = #{lineNo},</if>
            <if test="matCode != null">mat_code = #{matCode},</if>
            <if test="matName != null">mat_name = #{matName},</if>
            <if test="spec != null">spec = #{spec},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="deliveredQty != null">delivered_qty = #{deliveredQty},</if>
            <if test="unitPrice != null">unit_price = #{unitPrice},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="prodOrderNo != null">prod_order_no = #{prodOrderNo},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where detail_id = #{detailId}
    </update>

    <update id="deleteDetailByOrderNo">
        update stock_customer_order_detail set del_flag = '1' where order_no = #{orderNo}
    </update>

    <update id="deleteStockCustomerOrderDetailByDetailId" parameterType="Long">
        update stock_customer_order_detail set del_flag = '1' where detail_id = #{detailId}
    </update>

    <update id="deleteStockCustomerOrderDetailByDetailIds" parameterType="String">
        update stock_customer_order_detail set del_flag = '1' where detail_id in
        <foreach item="detailId" collection="array" open="(" separator="," close=")">
            #{detailId}
        </foreach>
    </update>
</mapper>

