第3章  	系统设计
3.1	总体架构设计
系统采用前后端分离架构。前端基于 Vue 与 Element UI 完成业务界面，包括入库/出库/退货/调拨单据录入、标签管理、扫码交互（若以页面形式实现则体现在输入/校验流程）以及库存查询与统计展示。后端基于 Spring Boot 与 MyBatis 完成业务逻辑与数据访问，并通过事务控制保障库存一致性更新。数据库使用 MySQL，按基础数据、库存业务与系统管理进行表结构划分。系统使用复用 RBAC 权限控制、菜单动态配置、登录日志与操作日志记录等能力。

图3-1 总体架构图

3.2	技术选型

本系统技术选型如表3-1所示，前端采用Vue.js+Element UI构建用户界面，后端采用Spring Boot+MyBatis实现业务逻辑和数据访问，数据库采用MySQL 5.7存储业务数据。

表3-1 系统技术选型表

| 技术分类 | 技术名称 | 版本 | 选型理由 |
|---------|---------|------|---------|
| **前端框架** | Vue.js | 2.6.14 | 渐进式框架，组件化开发，虚拟DOM提升性能 |
| **UI组件库** | Element UI | 2.15.13 | 组件丰富，界面美观，开发效率高 |
| **HTTP客户端** | Axios | 0.27.2 | 支持Promise，拦截器统一处理 |
| **路由管理** | Vue Router | 3.5.3 | 嵌套路由，懒加载，权限控制 |
| **状态管理** | Vuex | 3.6.2 | 集中式状态管理，变更可追踪 |
| **后端框架** | Spring Boot | 2.5.15 | 约定优于配置，内嵌容器 |
| **核心框架** | Spring Framework | 5.3.23 | IoC/AOP，声明式事务 |
| **持久层** | MyBatis | 3.5.10 | SQL灵活，性能优异 |
| **数据库** | MySQL | 5.7 | InnoDB事务，行级锁 |
| **连接池** | Druid | 1.2.11 | 性能优异，监控强大 |
| **身份认证** | JWT | - | 无状态认证，跨域友好 |

**开发工具**：IntelliJ IDEA、VS Code、Navicat、Postman、Git、Maven、npm

**运行环境**：JDK 1.8、Node.js 14.19.3、Nginx 1.18、Tomcat 9.0、MySQL 5.7

3.3	软件结构设计
本玻璃生产管理系统的功能模块图如下图3-2所示。

图3-2 功能模块图
本系统共有六个功能板块，分别为基础数据管理板块、采购管理板块、生产管理板块、库存管理板块、调拨管理板块、统计分析板块。其中，基础数据管理板块、采购管理板块、生产管理板块以及库存管理板块是本系统的核心板块。以下是对各个功能板块的介绍。
(1) 基础数据管理板块：该板块实现仓储管理基础信息的维护，支持物料信息、仓库信息、货位信息、供应商信息、车间信息等基础信息录入；可对基础信息进行修改、删除等操作，维护数据的准确性。
物料信息管理：针对仓储管理涉及的原料、半成品、成品等物料，建立标准化物料档案，记录物料编码、名称、规格型号、单位、分类、安全库存等信息，支持物料分类管理与版本追溯。
物料BOM管理：记录物料之间的组成关系，明确父物料与子物料对应关系及用量配比，支持BOM的创建、修改与版本控制，关联产品类型确保配方适配性。
物料分类管理：维护物料分类信息，实现物料的分级分类管理，支持分类的创建、修改与删除操作。
物料组别管理：维护物料组别信息，支持物料按组别归类，便于物料的分组管理与查询。
仓库管理：维护仓库基本信息、存储区域划分、仓库属性等，划分仓库功能区域，为物料存储提供精准的数据支持。
货位管理：维护货位编号信息，关联仓库与货位，明确货位归属，支持货位状态实时更新。
供应商管理：维护供应商基本信息、合作历史、供货质量评级，支持供应商资质审核与合作状态管理，为采购决策提供依据。
车间管理：维护车间基本信息，划分生产车间区域，为生产领料出库提供归属依据。
(2) 采购管理板块：围绕物料入库管理创建，支持创建入库单，录入入库仓库、入库货位、供应商信息、物料明细、入库数量等核心内容；对入库单进行修改、删除等操作，适配入库变更等场景。关联物料标签生成管理，记录入库状态（待入库、已入库、部分入库），跟踪入库单状态（正常完成、退回、作废）。
入库单管理：支持入库单创建、审批与入库确认，跟踪入库进度，记录入库时间、仓库、货位、数量及供应商信息，关联供应商评价体系。
入库退货管理：支持已入库物料的退货操作，记录退货原因、退货数量、退货时间等信息，自动更新库存数据。
物料标签管理：入库时自动生成物料标签，记录标签ID、物料编码、批次号、供应商、数量、存放仓库及货位等信息，支持标签打印与扫码操作。
(3) 生产管理板块：围绕生产领料出库管理创建，支持创建出库单，关联生产订单信息、领料车间信息、出库仓库、出库物料明细、出库数量等核心内容；对出库单进行修改、删除等操作，适配出库变更等场景。
生产订单管理：接收生产部门下达的生产订单，记录产品类型、规格、数量、交付日期等信息，支持订单审核、修改、取消及状态跟踪，关联出库单实现全流程追溯。
出库单管理：根据生产订单生成出库单，核对物料信息与订单需求，记录出库时间、经办人、领料车间，自动扣减库存，支持出库单打印与电子归档。
出库退货管理：支持已出库物料的退货操作，记录退货原因、退货数量、退货时间等信息，自动更新库存数据。
(4) 库存管理板块：该板块实现物料库存信息的全面管理，实时记录物料入库、出库、盘点数据，自动更新库存余量。
库存信息管理：实时记录物料在各仓库、货位的库存数量，自动更新库存余量，设置库存预警阈值，当物料低于阈值时触发提醒，支持物料批次追溯与保质期管理。
库存流水管理：记录物料出入库流水明细，包含流水类型（入库/出库/调拨）、物料信息、仓库信息、数量变化、操作时间、操作人等，支持按物料、仓库、时间段等多维度查询。
(5) 调拨管理板块：该板块整合了跨仓库物料调拨管理内容，支持创建调拨单，选择调出仓库、调出货位、调入仓库、调入货位、调拨物料及数量等；进行调拨单审核操作，完成物料跨仓库转移，并自动更新相关仓库的库存数据。
调拨单管理：支持调拨单创建、审批与调拨确认，跟踪调拨进度，记录调出仓库、调入仓库、调拨物料、调拨数量及调拨原因，通过系统化的调拨流程输出，降低仓库间的协调成本，增强仓库对物料调配的灵活性。
(6) 统计分析板块：该板块为用户提供多维度的统计分析功能，支持按时间段、仓库、供应商、车间、物料等多条件组合查询。
入库统计：按时间段、供应商、仓库、物料等维度统计入库情况，生成入库统计报表，帮助管理者掌握入库整体状况。
出库统计：按时间段、车间、仓库、物料等维度统计出库情况，生成出库统计报表，帮助管理者掌握出库整体状况。
库存流水统计：记录完整的库存变动明细，支持按物料、仓库、时间段等维度查询，实现库存变动的全程追溯。
3.3	类概要设计

图3-3 类概要设计图
在图 3-3 中，可看出本系统设计的基础类主要有15个，分别为 BaseMat（物料类）、BaseMatClass（物料分类类）、BaseMatGroup（物料组类）、BaseMatBom（物料BOM类）、BaseWarehouse（仓库类）、BaseLocation（货位类）、BaseSupplier（供应商类）、BaseWorkshop（车间类）、StockInfo（库存信息类）、StockMatLabel（物料标签类）、StockInOrder（入库单类）、StockOutOrder（出库单类）、StockProdOrder（生产订单类）、StockAllotOrder（调拨单类）、StockRecord（库存流水类）。
从用户登录仓储平台开始，用户的各类操作都需后端记录操作者的信息，并且返回给前端显示在页面上的信息也应携带用户信息。以 BaseMat（物料类）和 StockInfo（库存信息类）以及 StockMatLabel（物料标签类）为例，若物料螺丝已入库到一号仓库的A货位，在系统中就会生成相应的库存记录和物料标签，后台管理系统需要根据库存数量判断该物料是否需要进行补货提醒，这是仓储平台库存预警的合理逻辑。所以，BaseMat（物料类）依赖 StockInfo（库存信息类）和 StockMatLabel（物料标签类）。再看 BaseMat（物料类）与 StockInOrder（入库单类），用户创建入库单后，展现在页面上的除了入库单信息，还应有入库物料的基本信息。因此，BaseMat（物料类）依赖 StockInOrder（入库单类）。依此类推，用户在创建出库单、调拨单以及维护BOM时也是同样的情况，BaseMat（物料类）对 StockOutOrder（出库单类）、StockAllotOrder（调拨单类）以及 BaseMatBom（物料BOM类）也分别存在关联关系。
用户在创建入库单时，除了基本的单据信息外，还必须选择入库仓库、供应商以及具体物料明细，用于清晰记录物料的来源和存放位置。 用户在平台查看出库单以及对应的生产订单时，一个出库单可能会关联多个物料标签的出库操作，这些出库明细如何正确匹配物料标签，需要从数据库拿着出库明细中的物料编码去比对相应的物料标签库存。这个过程中，StockOutOrder（出库单类）依赖 StockMatLabel（物料标签类）。
物料在入库时，需要仓管员选择目标仓库和货位以及供应商信息，提交的入库信息应及时显示在页面上，此时页面上物料的所属仓库和货位不应该是数据库里记录的相应编号，而应该去 BaseWarehouse（仓库类）、BaseLocation（货位类）、BaseSupplier（供应商类）与之相对应的数据库表里查相应的仓库、货位、供应商记录，然后返回合适的物料入库信息的合适格式的信息。所以，StockInOrder（入库单类）分别依赖 BaseWarehouse（仓库类）、BaseLocation（货位类）、BaseSupplier（供应商类）和 BaseMat（物料类）。
3.4	数据库概要设计
本系统的 E-R 图如图 3-4 所示，在本实体关系图中，共有 10 个实体，分别是"物料"实体、"仓库"实体、"货位"实体、"供应商"实体、"车间"实体、"物料标签"实体、"入库单"实体、"出库单"实体、"调拨单"实体、"生产订单"实体。
其中，物料以 1：n 的"组成"关系对应物料BOM，即一个物料可作为多个BOM的父物料或子物料；而物料通过 1：1 的"归属"关系对接物料分类和物料组，实现一个物料归属于一个分类和一个组别；物料与库存信息呈 1：n 的"产生"关系，单个物料可产生多条库存记录；物料与物料标签以 1：n 的"标识"关系，满足单个物料可生成多个标签批次、一个标签对应一种物料。
仓储管理业务层面，仓库与货位为 1：n 的"包含"关系，一个仓库包含多个货位；入库单与入库明细以 1：n 的"明细"关系对应，一份入库单可包含多项入库明细；入库明细与物料标签呈 1：n 的"生成"关系，一条入库明细可生成多个物料标签。此外，出库单与生产订单为 1：n 的"关联"关系，即一个生产订单可关联多份出库单；出库单与出库明细为 1：n 的"明细"关系，出库明细与物料标签呈 n：1 的"消耗"关系，通过扫描物料标签完成物料出库。调拨单与调拨明细为 1：n 的"明细"关系，调拨明细通过 n：1 的"调出"关系关联源仓库和货位，同时通过 n：1 的"调入"关系关联目标仓库和货位，实现物料的跨仓库转移，通过这些实体关系进而全面构建起仓储管理业务的全流程逻辑，实现入库管理、出库管理、库存管理、调拨管理等核心业务的实体关联与映射


图3-4 数据库总体E-R图

3.5	核心模块详细设计

本节针对系统的四个核心模块进行详细设计，包括基础数据管理模块、入库管理模块、出库管理模块和库存管理与调拨模块。每个模块的详细设计包含类详细设计和数据库详细设计两部分内容。

### 3.5.1 基础数据管理模块详细设计

**（1）类详细设计**

【此处为基础数据管理模块类图】

基础数据管理模块共包含8个核心实体类，分别是：MatController（物料控制类）、MatService（物料服务接口）、MatServiceImpl（物料服务实现类）、BaseMat类（物料信息）、BaseMatClass类（物料分类）、BaseMatGroup类（物料组）、BaseMatBom类（物料BOM）、BaseWarehouse类（仓库信息）、BaseLocation类（货位信息）、BaseSupplier类（供应商信息）。其中BaseMat作为核心实体类，通过关联其他基础数据类共同构建完整的基础数据管理体系。

BaseMat类包含matCode（物料编码）、matName（物料名称）、figNum（图号）、specModel（规格型号）、matGroup（物料组编码，外键约束，关联物料组表）、matClass（物料分类编码，外键约束，关联物料分类表）、unit（计量单位）、safetyStock（安全库存）等属性。

BaseMatClass类包含classCode（分类编码）、className（分类名称）、parentCode（父分类编码）、orderNum（显示顺序）等属性，支持物料的多级分类管理。

BaseMatGroup类包含groupCode（组别编码）、groupName（组别名称）、remark（备注）等属性，支持物料按组别归类管理。

BaseMatBom类包含parentMatCode（父物料编码，外键约束，关联物料表）、matCode（子物料编码，外键约束，关联物料表）、quantity（用量）等属性，记录物料之间的组成关系，支持产品配方管理。

BaseWarehouse类包含warehouseCode（仓库编码）、warehouseName（仓库名称）、warehouseType（仓库类型）、warehouseKeeper（仓库负责人）等属性。

BaseLocation类包含locationCode（货位编码）、locationName（货位名称）、warehouseCode（所属仓库编码，外键约束，关联仓库表）等属性。

BaseSupplier类包含supplierCode（供应商编码）、supplierName（供应商名称）、contact（联系人）、phone（联系电话）、address（地址）等属性。

**（2）数据库详细设计**

基础数据管理模块主要用到了物料信息表、物料BOM表、仓库信息表。

表3-2 base_mat物料信息表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| mat_id | bigint(20) | 主键 |
| mat_code | varchar(64) | 物料编码 |
| mat_name | varchar(128) | 物料描述 |
| fd_code | varchar(64) | 财务编码 |
| fig_num | varchar(64) | 图号 |
| mat_group | varchar(32) | 物料组 |
| mat_class | varchar(32) | 分类 |
| unit_code | varchar(32) | 基本单位 |
| gross_weight | decimal(24,6) | 毛重 |
| safety_stock | decimal(24,6) | 安全库存 |
| standard_price | decimal(24,6) | 标准价 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

表3-3 base_mat_bom物料BOM表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| bom_id | bigint(20) | 主键 |
| father_mat_code | varchar(64) | 父物料编码 |
| father_mat_num | decimal(24,6) | 父项数量 |
| child_no | int(11) | 子项行项目号 |
| child_mat_code | varchar(64) | 子项物料编码 |
| child_mat_num | decimal(24,6) | 子项数量 |
| is_fictitious | char(1) | 是否虚拟键 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 更新时间 |

表3-4 base_warehouse仓库信息表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| warehouse_id | bigint(20) | 主键 |
| warehouse_code | varchar(64) | 仓库编码 |
| warehouse_name | varchar(128) | 仓库名称 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

base_mat表为物料信息表，字段包含物料基本属性，包括mat_code（物料编码）、mat_name（物料名称）、fd_code（财务编码）、fig_num（图号）、mat_group（物料组）、mat_class（分类）、unit_code（基本单位）、gross_weight（毛重）、safety_stock（安全库存）、standard_price（标准价）等。该表通过mat_group和mat_class字段分别关联物料组表和物料分类表，实现物料的分类管理；通过safety_stock字段支持库存预警功能，当物料库存低于安全库存时系统自动触发补货提醒。

base_mat_bom表为物料BOM表，记录物料之间的组成关系。字段包括father_mat_code（父物料编码）、father_mat_num（父项数量）、child_no（子项行项目号）、child_mat_code（子项物料编码）、child_mat_num（子项数量）、is_fictitious（是否虚拟键）等，通过外键关联物料表，支持产品配方的多级展开和BOM版本管理。

base_warehouse表为仓库信息表，字段包含warehouse_code（仓库编码）、warehouse_name（仓库名称）等基本信息，为物料存储提供仓库维度的数据支持。

### 3.5.2 入库管理模块详细设计

**（1）类详细设计**

【此处为入库管理模块类图】

入库管理模块共包含10个核心实体类，分别是：InOrderController（入库单控制类）、InOrderService（入库单服务接口）、InOrderServiceImpl（入库单服务实现类）、StockInOrder类（入库单信息）、StockInDetail类（入库明细）、StockMatLabel类（物料标签）、BaseMat类（物料信息）、BaseWarehouse类（仓库信息）、BaseLocation类（货位信息）、BaseSupplier类（供应商信息）。其中StockInOrder作为核心实体类，通过关联入库明细、物料标签及基础数据类共同构建完整的入库管理体系。

StockInOrder类包含orderNo（入库单号）、orderType（入库类型）、warehouseCode（仓库编码，外键约束，关联仓库表）、supplierCode（供应商编码，外键约束，关联供应商表）、orderStatus（单据状态）、warehouseKeeper（仓管员）等属性。

StockInDetail类包含orderNo（入库单号，外键约束，关联入库单表）、matCode（物料编码，外键约束，关联物料表）、locationCode（货位编码，外键约束，关联货位表）、planQuantity（计划数量）、actualQuantity（实际数量）、batchNo（批次号）等属性。

StockMatLabel类包含labelNo（标签编号）、warehouseCode（仓库编码）、locationCode（货位编码）、matCode（物料编码）、batchNo（批次号）、supplierCode（供应商编码）、labelQuantity（标签数量）、usableQuantity（可用数量）、inOrderNo（入库单号，外键约束，关联入库单表）、inType（入库类型）等属性。物料标签在入库确认时自动生成，记录物料的批次、供应商、存放位置等信息，支持物料的批次追溯和扫码管理。

**（2）数据库详细设计**

入库管理模块主要用到了入库单表、入库明细表、物料标签表。

表3-5 stock_in_order入库单表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| order_id | bigint(20) | 主键 |
| order_no | varchar(64) | 单据号 |
| order_type | varchar(32) | 单据类型 |
| order_status | varchar(32) | 单据状态 |
| check_status | varchar(32) | 检验状态 |
| check_by | varchar(64) | 检验员 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

表3-6 stock_in_detail入库明细表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| detail_id | bigint(20) | 主键 |
| warehouse_code | varchar(64) | 仓库编码 |
| order_no | varchar(64) | 入库单号 |
| line_no | int(11) | 行号 |
| mat_code | varchar(64) | 物料编码 |
| mat_name | varchar(128) | 物料名称 |
| fd_code | varchar(64) | 财务编码 |
| fig_num | varchar(64) | 图号 |
| mat_group | varchar(64) | 物料组 |
| mat_class | varchar(64) | 物料分类 |
| unit_code | varchar(32) | 单位 |
| label_id | varchar(64) | 物料标签id |
| batch | varchar(128) | 批次 |
| quantity | decimal(24,6) | 数量 |
| qualified_quantity | decimal(24,6) | 合格数量 |
| stock_in_quantity | decimal(24,6) | 入库数量 |
| unit_price | decimal(24,6) | 单价 |
| supplier_code | varchar(64) | 供应商编码 |
| supplier_name | varchar(128) | 供应商名称 |
| prod_time | datetime | 生产时间 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

表3-7 stock_mat_label物料标签表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| label_id | bigint(20) | 主键 |
| warehouse_code | varchar(64) | 仓库编码 |
| location_code | varchar(64) | 货位编码 |
| label_type | varchar(32) | 标签类型 |
| mat_code | varchar(64) | 物料编码 |
| mat_name | varchar(128) | 物料描述 |
| fd_code | varchar(64) | 财务编码 |
| fig_num | varchar(64) | 图号 |
| mat_group | varchar(64) | 物料组 |
| mat_class | varchar(64) | 分类 |
| unit_code | varchar(32) | 基本单位 |
| batch | varchar(128) | 批次 |
| supplier_code | varchar(64) | 供应商编码 |
| supplier_name | varchar(128) | 供应商名称 |
| prod_time | datetime | 生产时间 |
| quantity | decimal(24,6) | 数量 |
| usable_quantity | decimal(24,6) | 可用数量 |
| received_quantity | decimal(24,6) | 已领取数量 |
| unit_price | decimal(24,6) | 单价 |
| order_no | varchar(64) | 入库单号 |
| order_type | varchar(32) | 入库单类型 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

stock_in_order表为入库单表，字段包含order_no（单据号）、order_type（单据类型）、order_status（单据状态）、check_status（检验状态）、check_by（检验员）等属性。order_status字段记录单据状态，包括待入库、部分入库、已入库、已作废等状态，支持入库流程的状态跟踪。

stock_in_detail表为入库明细表，字段包括order_no（入库单号）、warehouse_code（仓库编码）、line_no（行号）、mat_code（物料编码）、mat_name（物料名称）、fd_code（财务编码）、fig_num（图号）、mat_group（物料组）、mat_class（物料分类）、unit_code（单位）、label_id（物料标签id）、batch（批次）、quantity（数量）、qualified_quantity（合格数量）、stock_in_quantity（入库数量）、unit_price（单价）、supplier_code（供应商编码）、supplier_name（供应商名称）、prod_time（生产时间）等。该表通过order_no关联入库单表，通过mat_code关联物料表，记录每个物料的入库明细信息，支持质检流程和价格记录。

stock_mat_label表为物料标签表，字段包括label_id（标签ID）、warehouse_code（仓库编码）、location_code（货位编码）、label_type（标签类型）、mat_code（物料编码）、mat_name（物料描述）、fd_code（财务编码）、fig_num（图号）、mat_group（物料组）、mat_class（分类）、unit_code（基本单位）、batch（批次）、supplier_code（供应商编码）、supplier_name（供应商名称）、prod_time（生产时间）、quantity（数量）、usable_quantity（可用数量）、received_quantity（已领取数量）、unit_price（单价）、order_no（入库单号）、order_type（入库单类型）等。该表在入库确认时自动生成标签记录，usable_quantity字段记录标签的可用数量，received_quantity字段记录已领取数量，支持物料的扫码出库和批次追溯管理。

### 3.5.3 出库管理模块详细设计

**（1）类详细设计**

【此处为出库管理模块类图】

出库管理模块共包含11个核心实体类，分别是：OutOrderController（出库单控制类）、OutOrderService（出库单服务接口）、OutOrderServiceImpl（出库单服务实现类）、StockOutOrder类（出库单信息）、StockOutDetail类（出库明细）、StockProdOrder类（生产订单）、StockMatLabel类（物料标签）、BaseMat类（物料信息）、BaseWarehouse类（仓库信息）、BaseLocation类（货位信息）、BaseWorkshop类（车间信息）。其中StockOutOrder作为核心实体类，通过关联生产订单、出库明细、物料标签及基础数据类共同构建完整的出库管理体系。

StockOutOrder类包含orderNo（出库单号）、orderType（出库类型）、prodOrderNo（生产订单号，外键约束，关联生产订单表）、warehouseCode（仓库编码，外键约束，关联仓库表）、workshopCode（车间编码，外键约束，关联车间表）、orderStatus（单据状态）、warehouseKeeper（仓管员）等属性。

StockOutDetail类包含orderNo（出库单号，外键约束，关联出库单表）、matCode（物料编码，外键约束，关联物料表）、locationCode（货位编码，外键约束，关联货位表）、planQuantity（计划数量）、actualQuantity（实际数量）、batchNo（批次号）等属性。

StockProdOrder类包含prodOrderNo（生产订单号）、prodMatCode（生产物料编码，外键约束，关联物料表）、prodQuantity（生产数量）、workshopCode（车间编码，外键约束，关联车间表）、planStartDate（计划开始日期）、planEndDate（计划结束日期）、orderStatus（订单状态）等属性。

**（2）数据库详细设计**

出库管理模块主要用到了出库单表、出库明细表、生产订单表。

表3-8 stock_out_order出库单表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| order_id | bigint(20) | 主键 |
| order_no | varchar(64) | 单据号 |
| order_type | varchar(32) | 单据类型 |
| prod_order_no | varchar(64) | 生产订单号 |
| warehouse_code | varchar(64) | 仓库编码 |
| workshop_code | varchar(64) | 车间编码 |
| order_reason | varchar(255) | 原因 |
| mat_code | varchar(64) | 物料编码 |
| mat_name | varchar(128) | 物料名称 |
| quantity | decimal(24,6) | 数量 |
| order_status | varchar(32) | 单据状态 |
| warehouse_keeper | varchar(64) | 库管员 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

表3-9 stock_out_detail出库明细表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| detail_id | bigint(20) | 主键 |
| warehouse_code | varchar(64) | 仓库编码 |
| workshop_code | varchar(64) | 车间编码 |
| location_code | varchar(64) | 货位编码 |
| prod_order_no | varchar(64) | 生产订单号 |
| order_no | varchar(64) | 出库单号 |
| line_no | int(11) | 行号 |
| mat_code | varchar(64) | 物料编码 |
| mat_name | varchar(128) | 物料名称 |
| fd_code | varchar(64) | 财务编码 |
| fig_num | varchar(64) | 图号 |
| mat_group | varchar(64) | 物料组 |
| mat_class | varchar(64) | 物料分类 |
| batch | varchar(128) | 批次 |
| quantity | decimal(24,6) | 数量 |
| received_quantity | decimal(24,6) | 已领数量 |
| unit_code | varchar(32) | 单位 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

表3-10 stock_prod_order生产订单表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| order_id | bigint(20) | 主键 |
| order_no | varchar(64) | 单据号 |
| work_no | varchar(64) | 工令号 |
| mat_code | varchar(64) | 物料编码 |
| mat_name | varchar(128) | 物料名称 |
| workshop_code | varchar(64) | 车间编码 |
| quantity | decimal(24,6) | 数量 |
| order_status | varchar(32) | 状态 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

stock_out_order表为出库单表，字段包含order_no（单据号）、order_type（单据类型）、prod_order_no（生产订单号）、warehouse_code（仓库编码）、workshop_code（车间编码）、order_reason（原因）、mat_code（物料编码）、mat_name（物料名称）、quantity（数量）、order_status（单据状态）、warehouse_keeper（库管员）等。该表通过prod_order_no关联生产订单表，通过warehouse_code关联仓库表，通过workshop_code关联车间表，记录物料出库的主单信息。系统支持根据生产订单自动展开BOM，生成出库单明细，简化出库单创建流程。

stock_out_detail表为出库明细表，字段包括order_no（出库单号）、prod_order_no（生产订单号）、warehouse_code（仓库编码）、workshop_code（车间编码）、location_code（货位编码）、mat_code（物料编码）、mat_name（物料名称）、fd_code（财务编码）、fig_num（图号）、mat_group（物料组）、mat_class（物料分类）、batch（批次）、quantity（数量）、received_quantity（已领数量）、unit_code（单位）等。该表通过order_no关联出库单表，通过mat_code关联物料表，记录每个物料的出库明细信息。系统支持货位推荐功能，根据先进先出原则自动推荐最优出库货位。

stock_prod_order表为生产订单表，字段包括order_no（单据号）、work_no（工令号）、mat_code（物料编码）、mat_name（物料名称）、workshop_code（车间编码）、quantity（数量）、order_status（状态）等。该表记录生产部门下达的生产订单信息，与出库单建立关联，支持物料领用的全流程追溯。

### 3.5.4 库存管理与调拨模块详细设计

**（1）类详细设计**

【此处为库存管理与调拨模块类图】

库存管理与调拨模块共包含10个核心实体类，分别是：StockInfoController（库存信息控制类）、StockInfoService（库存信息服务接口）、StockInfoServiceImpl（库存信息服务实现类）、AllotOrderController（调拨单控制类）、AllotOrderService（调拨单服务接口）、AllotOrderServiceImpl（调拨单服务实现类）、StockInfo类（库存信息）、StockRecord类（库存流水）、StockAllotOrder类（调拨单信息）、StockAllotDetail类（调拨明细）。其中StockInfo和StockAllotOrder作为核心实体类，共同构建完整的库存管理与调拨体系。

StockInfo类包含warehouseCode（仓库编码，外键约束，关联仓库表）、locationCode（货位编码，外键约束，关联货位表）、matCode（物料编码，外键约束，关联物料表）、batchNo（批次号）、quantity（库存数量）、supplierCode（供应商编码，外键约束，关联供应商表）等属性。该类记录物料在各仓库货位的实时库存数量，支持库存查询、安全库存预警和库存扣减等功能。

StockRecord类包含warehouseCode（仓库编码）、locationCode（货位编码）、labelId（物料标签ID）、recordType（流水类型）、matCode（物料编码）、batchNo（批次号）、quantity（变动数量）、orderNo（关联单号）、supplierCode（供应商编码）、workshopCode（车间编码）等属性。该类记录所有库存变动的历史记录，支持库存变动的全程追溯和审计。

StockAllotOrder类包含allotNo（调拨单号）、fromWarehouseCode（调出仓库编码，外键约束，关联仓库表）、fromLocationCode（调出货位编码）、toWarehouseCode（调入仓库编码，外键约束，关联仓库表）、toLocationCode（调入货位编码）、allotStatus（调拨状态）、allotProgress（调拨进度）等属性。

StockAllotDetail类包含allotNo（调拨单号，外键约束，关联调拨单表）、matCode（物料编码，外键约束，关联物料表）、batchNo（批次号）、allotQuantity（调拨数量）等属性。

**（2）数据库详细设计**

库存管理与调拨模块主要用到了库存信息表、库存流水表、调拨单表。

表3-11 stock_info库存信息表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| info_id | bigint(20) | 主键 |
| warehouse_code | varchar(64) | 仓库编码 |
| location_code | varchar(64) | 货位编码 |
| mat_code | varchar(64) | 物料编码 |
| mat_name | varchar(128) | 物料名称 |
| fd_code | varchar(64) | 财务编码 |
| fig_num | varchar(64) | 图号 |
| mat_group | varchar(64) | 物料组 |
| mat_class | varchar(64) | 物料分类 |
| unit_code | varchar(32) | 单位 |
| batch | varchar(128) | 批次 |
| quantity | decimal(24,6) | 数量 |
| supplier_code | varchar(64) | 供应商编码 |
| supplier_name | varchar(128) | 供应商名称 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

表3-12 stock_record库存流水表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| record_id | bigint(20) | 主键 |
| warehouse_code | varchar(64) | 仓库编码 |
| location_code | varchar(64) | 货位编码 |
| label_id | bigint(20) | 物料标签ID |
| record_type | varchar(32) | 流水类型 |
| mat_code | varchar(64) | 物料编码 |
| mat_name | varchar(128) | 物料名称 |
| fd_code | varchar(64) | 财务编码 |
| fig_num | varchar(64) | 图号 |
| mat_group | varchar(64) | 物料组 |
| mat_class | varchar(64) | 物料分类 |
| unit_code | varchar(32) | 单位 |
| batch | varchar(128) | 批次 |
| quantity | decimal(24,6) | 数量 |
| order_no | varchar(64) | 单据号 |
| workshop_code | varchar(64) | 车间编码 |
| supplier_code | varchar(64) | 供应商编码 |
| supplier_name | varchar(128) | 供应商名称 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

表3-13 stock_allot_order调拨单表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| allot_id | bigint(20) | 主键 |
| allot_no | varchar(64) | 调拨单号 |
| allot_reason | varchar(255) | 调拨原因 |
| src_warehouse_code | varchar(64) | 发起仓库编码 |
| dest_warehouse_code | varchar(64) | 目标仓库编码 |
| allot_status | varchar(32) | 调拨单状态 |
| allot_progress | varchar(64) | 调拨进度 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

表3-14 stock_allot_detail调拨明细表

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| detail_id | bigint(20) | 主键 |
| allot_no | varchar(64) | 调拨单号 |
| src_warehouse_code | varchar(64) | 发起仓库 |
| dest_warehouse_code | varchar(64) | 目标仓库 |
| src_location_code | varchar(64) | 发起货位 |
| dest_location_code | varchar(64) | 目标货位 |
| line_no | int(11) | 行号 |
| label_id | varchar(64) | 物料标签id |
| mat_code | varchar(64) | 物料编码 |
| mat_name | varchar(128) | 物料名称 |
| fd_code | varchar(64) | 财务编码 |
| fig_num | varchar(64) | 图号 |
| mat_group | varchar(64) | 物料组 |
| mat_class | varchar(64) | 物料分类 |
| batch | varchar(128) | 批次 |
| quantity | decimal(24,6) | 数量 |
| sign_quantity | decimal(24,6) | 签收数量 |
| unit_code | varchar(32) | 单位 |
| supplier_code | varchar(64) | 供应商编码 |
| supplier_name | varchar(128) | 供应商名称 |
| del_flag | char(1) | 删除标识 |
| create_by | varchar(64) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(64) | 修改人 |
| update_time | datetime | 修改时间 |

stock_info表为库存信息表，字段包含warehouse_code（仓库编码）、location_code（货位编码）、mat_code（物料编码）、mat_name（物料名称）、fd_code（财务编码）、fig_num（图号）、mat_group（物料组）、mat_class（物料分类）、unit_code（单位）、batch（批次）、quantity（数量）、supplier_code（供应商编码）、supplier_name（供应商名称）等。该表记录物料在各仓库货位的实时库存数量，支持库存查询、安全库存预警和库存扣减等功能。系统在入库、出库、调拨等操作时自动更新该表的quantity字段，确保库存数据的实时性和准确性。

stock_record表为库存流水表，字段包括warehouse_code（仓库编码）、location_code（货位编码）、label_id（物料标签ID）、record_type（流水类型）、mat_code（物料编码）、mat_name（物料名称）、fd_code（财务编码）、fig_num（图号）、mat_group（物料组）、mat_class（物料分类）、unit_code（单位）、batch（批次）、quantity（数量）、order_no（单据号）、workshop_code（车间编码）、supplier_code（供应商编码）、supplier_name（供应商名称）等。该表记录每一次库存变动的详细信息，record_type字段标识流水类型（入库、出库、调拨、盘点等），quantity字段记录变动数量（正数表示增加，负数表示减少），order_no字段关联业务单据，支持库存变动的全程追溯。

stock_allot_order表为调拨单表，字段包括allot_no（调拨单号）、allot_reason（调拨原因）、src_warehouse_code（发起仓库编码）、dest_warehouse_code（目标仓库编码）、allot_status（调拨单状态）、allot_progress（调拨进度）等。该表记录物料跨仓库调拨的主单信息，allot_status字段记录调拨状态（待审核、已审核、已完成、已作废），allot_progress字段记录调拨进度（待调出、已调出待调入、已完成），支持调拨流程的精细化管理。

stock_allot_detail表为调拨明细表，字段包括allot_no（调拨单号）、src_warehouse_code（发起仓库）、dest_warehouse_code（目标仓库）、src_location_code（发起货位）、dest_location_code（目标货位）、line_no（行号）、label_id（物料标签id）、mat_code（物料编码）、mat_name（物料名称）、fd_code（财务编码）、fig_num（图号）、mat_group（物料组）、mat_class（物料分类）、batch（批次）、quantity（数量）、sign_quantity（签收数量）、unit_code（单位）、supplier_code（供应商编码）、supplier_name（供应商名称）等。该表通过allot_no关联调拨单表，通过mat_code关联物料表，记录每个物料的调拨明细信息，sign_quantity字段记录目标仓库的签收数量，支持调拨过程的精细化跟踪和物料批次追溯。

3.6	本章小结

本章从系统总体设计的角度，全面阐述了系统的架构设计、技术选型、功能模块划分、类设计和数据库设计。首先介绍了系统采用的前后端分离三层架构模式，明确了表现层、业务逻辑层和数据访问层的职责划分。其次，通过表格形式简洁地展示了系统的技术选型，说明了前端、后端、数据库等各项技术的选型理由和开发运行环境。然后，详细介绍了系统的六大功能模块，包括基础数据管理、采购管理、生产管理、库存管理、调拨管理和统计分析，明确了各模块的核心功能和业务逻辑。接着，通过类概要设计图展示了系统15个核心类之间的依赖关系，为系统实现提供了面向对象设计基础。在数据库概要设计部分，通过E-R图展示了系统10个核心实体及其关系，明确了实体间的联系类型和业务语义。最后，在核心模块详细设计部分，针对基础数据管理、入库管理、出库管理和库存管理与调拨四个核心模块，分别从类详细设计和数据库详细设计两个层面进行了深入阐述，详细说明了各模块涉及的实体类属性、关联关系以及数据表字段语义和外键关联结构，为第四章系统实现提供了完整的设计依据和理论基础。
