# 5.3 出库管理模块实现（生产订单、货位推荐、扫码出库）

## 5.3.1 流程图

生产计划员登录系统后进入生产订单管理模块，创建生产订单，填写产品类型、规格、数量、交付日期等信息，系统生成唯一的生产订单号并保存至数据库，订单状态设置为"已创建"。基于生产订单，系统自动或手动创建出库单，选择出库类型（生产领料、补领出库或销售出库），填写出库仓库、领料车间等基本信息。系统根据生产订单关联的产品BOM信息展开物料清单，自动生成出库明细，包括所需物料编码、名称、图号及出库数量。出库单创建完成后，状态更新为"已创建"。仓管员进入出库单详情页面，系统为每条出库明细推荐货位，推荐逻辑基于该物料在指定仓库中库存数量最多的货位，若无库存则显示"暂无库存"。仓管员使用扫码设备扫描出库单二维码进入扫码出库界面，依次扫描物料标签二维码，系统自动匹配出库明细并累加扫码数量，同时校验标签的仓库和货位是否与推荐货位一致。确认所有物料扫码完成后，仓管员提交出库操作，系统执行库存扣减逻辑：扣减物料标签的可用数量；扣减库存信息表中对应仓库货位的库存数量；新增库存流水记录，记录类型为出库，记录数量为负值表示库存减少。最后，系统将出库单状态修改为"已出库"，流程结束。若扫码过程中发现标签不匹配或库存不足，系统提示错误信息，仓管员可调整后重新扫码。

```plantuml
@startuml
title 出库管理业务流程

start
:生产计划员登录系统;
:进入生产订单管理;

partition "创建生产订单" {
  :点击新增生产订单;
  :填写订单信息;
  note right
    - 产品类型
    - 规格
    - 数量
    - 交付日期
  end note
  :生成生产订单号;
  :订单状态设置为已创建;
  :保存生产订单;
}

partition "创建出库单" {
  :基于生产订单创建出库单;
  :选择出库类型;
  note right
    - 生产领料
    - 补领出库
    - 销售出库
  end note
  :填写基本信息;
  note right
    - 出库仓库
    - 领料车间
    - 出库原因
  end note
  :根据BOM展开物料清单;
  :自动生成出库明细;
  :提交出库单;
  :状态更新为已创建;
}

partition "货位推荐" {
  :仓管员进入出库单详情;
  :系统加载出库明细;
  
  repeat
    :获取物料编码和仓库编码;
    :查询该物料库存信息;
    if (存在库存?) then (是)
      :推荐库存最多的货位;
      :更新明细货位信息;
    else (否)
      :显示暂无库存提示;
    endif
  repeat while (所有明细处理完成?) is (否)
  -> 是;
  
  :显示带推荐货位的明细列表;
}

partition "扫码出库" {
  :仓管员扫描出库单二维码;
  :进入扫码出库界面;
  :显示出库明细列表;
  
  repeat
    :扫描物料标签二维码;
    :解析标签信息;
    :匹配出库明细;
    
    if (匹配成功?) then (是)
      :校验仓库货位;
      if (货位一致?) then (是)
        :校验可用数量;
        if (数量充足?) then (是)
          :累加扫码数量;
          :更新明细显示;
        else (否)
          :提示库存不足;
        endif
      else (否)
        :提示货位不匹配;
      endif
    else (否)
      :提示标签错误;
    endif
  repeat while (所有物料扫码完成?) is (否)
  -> 是;
  
  :确认提交出库;
}

partition "库存扣减" {
  :开启事务处理;
  
  repeat
    :扣减物料标签可用数量;
    :扣减库存信息数量;
    if (库存扣减成功?) then (是)
      :新增库存流水记录;
      note right
        记录类型：出库
        记录数量：负值
      end note
      :更新出库明细已出库数量;
    else (否)
      :回滚事务;
      :返回库存不足错误;
      stop
    endif
  repeat while (明细处理完成?) is (否)
  -> 是;
  
  :更新出库单状态为已出库;
  :提交事务;
}

:出库完成;
stop

@enduml
```

## 5.3.2 顺序图

```plantuml
@startuml
title 扫码出库顺序图

actor 仓管员
participant "前端页面" as Frontend
participant "OutOrderController" as Controller
participant "OutOrderServiceImpl" as Service
participant "MatLabelMapper" as LabelMapper
participant "StockInfoMapper" as InfoMapper
participant "StockRecordMapper" as RecordMapper
participant "OutDetailMapper" as DetailMapper
database "MySQL数据库" as DB

仓管员 -> Frontend: 扫描出库单二维码
Frontend -> Controller: GET /stock/outOrder/m/{orderNo}
Controller -> Service: selectStockOutOrderByOrderNo(orderNo)
Service -> DB: 查询出库单信息
DB --> Service: 返回出库单数据
Service -> DB: 查询出库明细列表
DB --> Service: 返回明细列表
Service -> Service: 过滤已完成的明细
Service --> Controller: 返回出库单对象
Controller --> Frontend: 返回出库单及未完成明细

Frontend -> Frontend: 显示出库明细
仓管员 -> Frontend: 扫描物料标签二维码

loop 扫描每个物料标签
  Frontend -> Frontend: 解析标签编号
  Frontend -> Frontend: 匹配出库明细
  Frontend -> Frontend: 校验仓库货位
  Frontend -> Frontend: 校验可用数量
  Frontend -> Frontend: 累加扫码数量
  Frontend -> Frontend: 更新界面显示
end

仓管员 -> Frontend: 确认提交出库
Frontend -> Controller: POST /stock/outOrder/submitStockOut

Controller -> Service: submitStockOut(username, requestBody)
Service -> Service: 开启事务处理
Service -> Service: 获取出库类型和流水类型

loop 处理每个扫描的标签
  Service -> LabelMapper: updateUsableQuantity(labelId, quantity)
  LabelMapper -> DB: UPDATE stock_mat_label\nSET usable_quantity = usable_quantity - ?
  DB --> LabelMapper: 返回更新结果
  
  Service -> InfoMapper: updateQuantity(warehouse, location, ...)
  InfoMapper -> DB: UPDATE stock_info\nSET quantity = quantity - ?
  DB --> InfoMapper: 返回更新结果
  
  Service -> RecordMapper: insertStockRecord(record)
  RecordMapper -> DB: INSERT INTO stock_record\nVALUES (...)
  DB --> RecordMapper: 返回插入结果
  
  Service -> DetailMapper: updateReceivedQuantity(detailId, quantity)
  DetailMapper -> DB: UPDATE stock_out_detail\nSET received_quantity = received_quantity + ?
  DB --> DetailMapper: 返回更新结果
end

Service -> Service: 检查出库单是否完成
alt 所有明细已完成
  Service -> DB: UPDATE stock_out_order\nSET order_status = '已出库'
else 部分明细完成
  Service -> DB: UPDATE stock_out_order\nSET order_status = '部分出库'
end

Service -> Service: 提交事务
Service --> Controller: 返回操作结果

Controller --> Frontend: 返回成功响应
Frontend -> Frontend: 显示成功提示
Frontend -> Frontend: 返回出库单列表
Frontend --> 仓管员: 出库完成

@enduml
```

仓管员使用扫码设备扫描出库单二维码，前端页面向OutOrderController发送GET请求获取出库单信息。Controller调用Service层的selectStockOutOrderByOrderNo方法查询出库单详细信息。Service层从数据库查询出库单主表信息和出库明细列表，过滤掉已完成的明细（已出库数量等于应出库数量的明细），只返回未完成或部分完成的明细，Controller将数据返回给前端。前端页面显示出库明细列表，包括物料编码、名称、应出库数量、已出库数量和推荐货位等信息。仓管员依次扫描物料标签二维码，前端解析标签编号，在出库明细列表中匹配对应的物料，校验标签的仓库和货位是否与出库明细一致，校验标签的可用数量是否充足，校验通过后累加扫码数量并实时更新界面显示。所有物料扫码完成后，仓管员点击确认提交按钮，前端向Controller发送POST请求执行出库操作。Controller调用Service层的submitStockOut方法，Service开启数据库事务处理，获取出库类型并根据出库类型确定库存流水记录类型。对于每个扫描的物料标签，Service首先调用MatLabelMapper扣减物料标签的可用数量；然后调用InfoMapper扣减库存信息表中对应仓库货位的库存数量；接着调用RecordMapper新增库存流水记录，记录类型为出库，记录数量为负值表示库存减少；最后调用DetailMapper更新出库明细的已出库数量。所有标签处理完成后，Service检查出库单是否完成，若所有明细的已出库数量都等于应出库数量则更新出库单状态为"已出库"，若仍有未完成的明细则更新状态为"部分出库"。Service提交事务并返回操作结果，Controller将成功响应返回给前端，前端显示操作成功提示并返回出库单列表页面，出库流程结束。

## 5.3.3 数据流图

```plantuml
@startuml
title 出库管理数据流图 (DFD)

skinparam rectangle {
  BackgroundColor<<external>> #E8F5E9
  BackgroundColor<<process>> #E3F2FD
  BackgroundColor<<storage>> #FFF3E0
  BorderColor #666666
  FontSize 12
}

' 外部实体
rectangle "生产计划员\n/仓管员" as User <<external>>
rectangle "前端页面\n(Vue.js)" as Frontend <<external>>
rectangle "扫码设备" as Scanner <<external>>

' 数据处理过程
rectangle "P1\n生产订单处理\n(ProdOrderController)" as P1 <<process>>
rectangle "P2\n出库单处理\n(OutOrderController)" as P2 <<process>>
rectangle "P3\n货位推荐处理\n(OutOrderService)" as P3 <<process>>
rectangle "P4\n扫码出库处理\n(OutOrderService)" as P4 <<process>>
rectangle "P5\n库存扣减处理\n(StockMapper)" as P5 <<process>>

' 数据存储
rectangle "D1\nprod_order\n(生产订单表)" as D1 <<storage>>
rectangle "D2\nstock_out_order\n(出库单主表)" as D2 <<storage>>
rectangle "D3\nstock_out_detail\n(出库明细表)" as D3 <<storage>>
rectangle "D4\nstock_mat_label\n(物料标签表)" as D4 <<storage>>
rectangle "D5\nstock_info\n(库存信息表)" as D5 <<storage>>
rectangle "D6\nstock_record\n(库存流水表)" as D6 <<storage>>
rectangle "D7\nbase_mat_bom\n(物料BOM表)" as D7 <<storage>>

' 数据流
User --> Frontend : 生产订单数据
Frontend --> P1 : HTTP请求
P1 --> D1 : 插入生产订单
D1 --> P1 : 订单信息

Frontend --> P2 : 出库单数据
P2 --> D1 : 查询生产订单
D1 --> P2 : 订单产品信息
P2 --> D7 : 查询BOM清单
D7 --> P2 : 物料清单数据
P2 --> D2 : 插入出库单
P2 --> D3 : 插入出库明细

Frontend --> P3 : 货位推荐请求
P3 --> D3 : 查询出库明细
D3 --> P3 : 明细物料信息
P3 --> D5 : 查询库存分布
D5 --> P3 : 各货位库存数量
P3 --> Frontend : 推荐货位信息

Scanner --> Frontend : 标签二维码
Frontend --> P4 : 扫码出库请求
P4 --> D2 : 查询出库单
D2 --> P4 : 出库单数据
P4 --> D3 : 查询出库明细
D3 --> P4 : 明细数据

P4 --> P5 : 出库数据处理
P5 --> D4 : 扣减标签可用数量
P5 --> D5 : 扣减库存数量
P5 --> D6 : 插入出库流水
P5 --> D3 : 更新已出库数量

P4 --> D2 : 更新订单状态

P5 --> P4 : 处理结果
P4 --> Frontend : 出库结果
Frontend --> User : 界面展示

note right of P1
  创建生产订单
  生成订单号
end note

note right of P2
  创建出库单
  BOM展开物料清单
  自动生成出库明细
end note

note right of P3
  查询各货位库存
  推荐库存最多货位
end note

note right of P4
  扫码匹配明细
  校验仓库货位
  @Transactional事务
end note

note right of P5
  扣减标签可用数量
  扣减库存数量
  记录出库流水
end note

@enduml
```

生产计划员通过前端页面创建生产订单，请求数据流入P1生产订单处理过程，P1生成订单号，设置状态为"已创建"，将订单数据写入prod_order表。计划员基于生产订单创建出库单，出库单数据流入P2出库单处理过程，若关联生产订单则P2从prod_order表查询产品信息，从base_mat_bom表查询BOM清单，展开物料清单自动生成出库明细，将出库单数据写入stock_out_order表和stock_out_detail表。仓管员进入出库单详情页面，货位推荐请求流入P3货位推荐处理过程，P3从stock_out_detail表查询物料信息，从stock_info表查询各货位库存分布，选择库存最多的货位作为推荐货位返回前端显示。仓管员使用扫码设备扫描出库单二维码进入扫码出库界面，依次扫描物料标签二维码，前端解析标签编号并在本地匹配出库明细、校验仓库货位、校验可用数量、累加扫码数量。扫码完成后确认提交出库，扫码数据流入P4扫码出库处理过程，P4开启数据库事务，调用P5库存扣减处理过程执行库存扣减逻辑。对于每个扫描的物料标签，P5从stock_mat_label表扣减可用数量，从stock_info表扣减库存数量，向stock_record表插入出库流水记录，更新stock_out_detail表的已出库数量。所有标签处理完成后，P4检查出库单完成情况，更新stock_out_order表状态为"已出库"或"部分出库"，提交事务返回处理结果。整个数据流清晰地展示了出库业务从生产订单创建、出库单生成、货位推荐到扫码出库、库存扣减的完整流转过程。
