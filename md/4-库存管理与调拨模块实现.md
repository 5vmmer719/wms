# 5.4 库存管理与调拨模块实现

## 5.4.1 流程图

管理员或仓管员登录系统后进入库存管理模块，可选择查看库存信息或库存流水。在库存信息查询功能中，用户输入查询条件（物料编码、仓库编码、货位编码等），系统从库存信息表（stock_info）中查询符合条件的库存记录，支持按物料、仓库、货位等多维度统计汇总。系统展示每个库存记录的详细信息，包括物料编码、名称、批次号、供应商、仓库、货位、库存数量等。用户可设置安全库存预警阈值，当物料库存低于阈值时系统自动标红提示，便于及时补货。在库存流水查询功能中，用户选择时间范围和查询条件，系统从库存流水表（stock_record）中查询历史流水记录，展示每次库存变动的详细信息，包括流水类型、物料信息、仓库货位、变动数量、关联单号、操作人和操作时间等。流水记录按时间倒序排列，支持导出Excel报表。

对于库存调拨功能，仓管员创建调拨单，选择调出仓库、调出货位、调入仓库、调入货位，添加调拨物料及数量。调拨单创建完成后，状态设置为"待调拨"，调拨进度为"未调出"。仓管员扫描调拨单二维码进入扫码调出界面，扫描物料标签完成调出操作，系统扣减调出仓库货位的库存，新增调拨出库流水记录，调拨进度更新为"已调出"。随后仓管员扫描调拨单二维码进入扫码调入界面，扫描物料标签完成调入操作，系统增加调入仓库货位的库存，新增调拨入库流水记录，调拨进度更新为"已调入"，调拨单状态更新为"已完成"，流程结束。

```plantuml
@startuml
title 库存管理与调拨业务流程

start

if (功能选择?) then (库存信息查询)
  :进入库存信息管理;
  :输入查询条件;
  note right
    - 物料编码
    - 仓库编码
    - 货位编码
    - 批次号
  end note
  :查询库存信息表;
  :展示库存记录;
  :计算库存汇总;
  
  if (库存低于安全库存?) then (是)
    :标红显示预警;
    :提示需要补货;
  else (否)
    :正常显示;
  endif
  
  :支持导出Excel;
  
elseif (库存流水查询) then
  :进入库存流水管理;
  :选择时间范围;
  :输入查询条件;
  note right
    - 物料编码
    - 仓库编码
    - 流水类型
    - 关联单号
  end note
  :查询库存流水表;
  :展示流水记录;
  note right
    按时间倒序排列
    显示变动详情
  end note
  :支持导出Excel;
  
else (库存调拨)
  partition "创建调拨单" {
    :点击新增调拨单;
    :选择调出仓库和货位;
    :选择调入仓库和货位;
    :填写调拨原因;
    :添加调拨物料明细;
    :填写调拨数量;
    :提交调拨单;
    :状态设置为待调拨;
    :调拨进度为未调出;
  }
  
  partition "扫码调出" {
    :仓管员扫描调拨单二维码;
    :进入扫码调出界面;
    :显示调拨明细列表;
    
    repeat
      :扫描物料标签二维码;
      :匹配调拨明细;
      if (匹配成功?) then (是)
        :校验调出仓库货位;
        if (货位一致?) then (是)
          :校验可用数量;
          if (数量充足?) then (是)
            :累加扫码数量;
            :更新明细显示;
          else (否)
            :提示库存不足;
          endif
        else (否)
          :提示货位不匹配;
        endif
      else (否)
        :提示标签错误;
      endif
    repeat while (所有物料扫码完成?) is (否)
    -> 是;
    
    :确认提交调出;
    :扣减调出仓库库存;
    :新增调拨出库流水;
    :调拨进度更新为已调出;
  }
  
  partition "扫码调入" {
    :仓管员扫描调拨单二维码;
    :进入扫码调入界面;
    :显示调拨明细列表;
    
    repeat
      :扫描物料标签二维码;
      :匹配调拨明细;
      :累加扫码数量;
      :更新明细显示;
    repeat while (所有物料扫码完成?) is (否)
    -> 是;
    
    :确认提交调入;
    :增加调入仓库库存;
    :新增调拨入库流水;
    :调拨进度更新为已调入;
    :调拨单状态更新为已完成;
  }
endif

:操作完成;
stop

@enduml
```

## 5.4.2 顺序图

```plantuml
@startuml
title 库存调拨顺序图

actor 仓管员
participant "前端页面" as Frontend
participant "AllotOrderController" as Controller
participant "AllotOrderServiceImpl" as Service
participant "AllotDetailMapper" as DetailMapper
participant "StockInfoMapper" as InfoMapper
participant "StockRecordMapper" as RecordMapper
database "MySQL数据库" as DB

仓管员 -> Frontend: 点击新增调拨单
Frontend -> Frontend: 弹出表单对话框
仓管员 -> Frontend: 填写调拨信息
note right
  - 调出仓库和货位
  - 调入仓库和货位
  - 调拨物料和数量
  - 调拨原因
end note

Frontend -> Controller: POST /stock/allotOrder/add
Controller -> Service: insertStockAllotOrder(username, allotOrder)
Service -> Service: 生成调拨单号
Service -> Service: 设置状态为待调拨
Service -> Service: 设置进度为未调出

Service -> DB: INSERT INTO stock_allot_order
DB --> Service: 返回插入结果
Service -> DetailMapper: insertAllotDetailList(detailList)
DetailMapper -> DB: INSERT INTO stock_allot_detail
DB --> DetailMapper: 返回插入结果

Service --> Controller: 返回操作结果
Controller --> Frontend: 返回成功响应
Frontend --> 仓管员: 显示创建成功

== 扫码调出阶段 ==

仓管员 -> Frontend: 扫描调拨单二维码(调出)
Frontend -> Controller: GET /stock/allotOrder/m/{allotNo}
Controller -> Service: selectStockAllotOrderByAllotNo(allotNo)
Service -> DB: 查询调拨单信息
DB --> Service: 返回调拨单数据
Service --> Controller: 返回调拨单对象
Controller --> Frontend: 返回调拨单及明细

Frontend -> Frontend: 显示调拨明细
仓管员 -> Frontend: 扫描物料标签
Frontend -> Frontend: 匹配明细并累加数量

仓管员 -> Frontend: 确认提交调出
Frontend -> Controller: POST /stock/allotOrder/submitAllotOut

Controller -> Service: submitAllotOut(username, requestBody)
Service -> Service: 开启事务处理

loop 处理每个扫描的标签
  Service -> InfoMapper: updateQuantity(调出仓库, ...)
  InfoMapper -> DB: UPDATE stock_info\nSET quantity = quantity - ?
  
  Service -> RecordMapper: insertStockRecord(调出流水)
  RecordMapper -> DB: INSERT INTO stock_record\n(record_type='allot_out')
  
  Service -> DetailMapper: updateAllotQuantity(detailId, ...)
  DetailMapper -> DB: UPDATE stock_allot_detail\nSET allot_quantity = ?
end

Service -> DB: UPDATE stock_allot_order\nSET allot_progress = '已调出'
Service -> Service: 提交事务
Service --> Controller: 返回操作结果
Controller --> Frontend: 返回成功响应

== 扫码调入阶段 ==

仓管员 -> Frontend: 扫描调拨单二维码(调入)
Frontend -> Controller: GET /stock/allotOrder/m/{allotNo}
Controller -> Service: selectStockAllotOrderByAllotNo(allotNo)
Service --> Controller: 返回调拨单数据
Controller --> Frontend: 返回调拨单及明细

Frontend -> Frontend: 显示调拨明细
仓管员 -> Frontend: 扫描物料标签
Frontend -> Frontend: 匹配明细并累加数量

仓管员 -> Frontend: 确认提交调入
Frontend -> Controller: POST /stock/allotOrder/submitAllotIn

Controller -> Service: submitAllotIn(username, requestBody)
Service -> Service: 开启事务处理

loop 处理每个扫描的标签
  Service -> InfoMapper: 查询调入仓库库存
  InfoMapper -> DB: SELECT * FROM stock_info
  
  alt 库存记录存在
    Service -> InfoMapper: updateStockInfo(累加数量)
    InfoMapper -> DB: UPDATE stock_info\nSET quantity = quantity + ?
  else 库存记录不存在
    Service -> InfoMapper: insertStockInfo(新增记录)
    InfoMapper -> DB: INSERT INTO stock_info
  end
  
  Service -> RecordMapper: insertStockRecord(调入流水)
  RecordMapper -> DB: INSERT INTO stock_record\n(record_type='allot_in')
  
  Service -> DetailMapper: updateSignQuantity(detailId, ...)
  DetailMapper -> DB: UPDATE stock_allot_detail\nSET sign_quantity = ?
end

Service -> DB: UPDATE stock_allot_order\nSET allot_progress = '已调入'\nSET allot_status = '已完成'
Service -> Service: 提交事务
Service --> Controller: 返回操作结果
Controller --> Frontend: 返回成功响应
Frontend --> 仓管员: 调拨完成

@enduml
```

仓管员点击新增调拨单按钮，前端页面弹出表单对话框。仓管员填写调拨信息，包括调出仓库和货位、调入仓库和货位、调拨物料和数量、调拨原因等。前端向AllotOrderController发送POST请求（/stock/allotOrder/add），Controller调用Service层的insertStockAllotOrder方法处理业务逻辑。Service生成唯一的调拨单号，设置调拨状态为"待调拨"，设置调拨进度为"未调出"，向数据库插入调拨单主表记录（INSERT INTO stock_allot_order），数据库返回插入结果。随后Service调用DetailMapper的insertAllotDetailList方法批量插入调拨明细数据（INSERT INTO stock_allot_detail），数据库返回插入结果。Service将操作结果返回给Controller，Controller将成功响应返回给前端，前端显示创建成功提示。

在扫码调出阶段，仓管员扫描调拨单二维码进入调出界面，前端向Controller发送GET请求（/stock/allotOrder/m/{allotNo}）获取调拨单信息。Controller调用Service的selectStockAllotOrderByAllotNo方法查询调拨单信息，Service从数据库查询调拨单数据，数据库返回调拨单数据，Service将调拨单对象返回给Controller，Controller将调拨单及明细信息返回给前端。前端显示调拨明细列表，仓管员依次扫描物料标签，前端在本地匹配调拨明细并累加扫码数量。仓管员确认提交调出操作，前端向Controller发送POST请求（/stock/allotOrder/submitAllotOut）。Controller调用Service的submitAllotOut方法，Service开启数据库事务处理。Service遍历每个扫描的物料标签，调用InfoMapper的updateQuantity方法扣减调出仓库的库存数量（UPDATE stock_info SET quantity = quantity - ? WHERE warehouse_code = ? AND location_code = ?），调用RecordMapper的insertStockRecord方法新增调拨出库流水记录（INSERT INTO stock_record，流水类型为allot_out），调用DetailMapper的updateAllotQuantity方法更新调拨明细的已调出数量（UPDATE stock_allot_detail SET allot_quantity = ?）。所有标签处理完成后，Service更新调拨单的调拨进度为"已调出"（UPDATE stock_allot_order SET allot_progress = '已调出'），提交事务，将操作结果返回给Controller，Controller将成功响应返回给前端。

在扫码调入阶段，仓管员再次扫描调拨单二维码进入调入界面，前端向Controller发送GET请求获取调拨单信息。Controller调用Service查询调拨单及明细数据并返回给前端显示。前端显示调拨明细列表，仓管员依次扫描物料标签，前端在本地匹配调拨明细并累加扫码数量。仓管员确认提交调入操作，前端向Controller发送POST请求（/stock/allotOrder/submitAllotIn）。Controller调用Service的submitAllotIn方法，Service开启数据库事务处理。Service遍历每个扫描的物料标签，首先调用InfoMapper查询调入仓库货位的库存记录（SELECT * FROM stock_info），若库存记录存在则调用InfoMapper的updateStockInfo方法累加库存数量（UPDATE stock_info SET quantity = quantity + ?），若库存记录不存在则调用InfoMapper的insertStockInfo方法新增库存记录（INSERT INTO stock_info）。随后调用RecordMapper的insertStockRecord方法新增调拨入库流水记录（INSERT INTO stock_record，流水类型为allot_in），调用DetailMapper的updateSignQuantity方法更新调拨明细的已调入数量（UPDATE stock_allot_detail SET sign_quantity = ?）。所有标签处理完成后，Service更新调拨单的调拨进度为"已调入"，调拨状态为"已完成"（UPDATE stock_allot_order SET allot_progress = '已调入', allot_status = '已完成'），提交事务，将操作结果返回给Controller，Controller将成功响应返回给前端，前端显示调拨完成提示，整个调拨流程结束。

## 5.4.3 数据流图

```plantuml
@startuml
title 库存管理与调拨数据流图 (DFD)

skinparam rectangle {
  BackgroundColor<<external>> #E8F5E9
  BackgroundColor<<process>> #E3F2FD
  BackgroundColor<<storage>> #FFF3E0
  BorderColor #666666
  FontSize 12
}

' 外部实体
rectangle "管理员/仓管员" as User <<external>>
rectangle "前端页面\n(Vue.js)" as Frontend <<external>>
rectangle "扫码设备" as Scanner <<external>>

' 数据处理过程
rectangle "P1\n库存查询处理\n(StockInfoService)" as P1 <<process>>
rectangle "P2\n流水查询处理\n(StockRecordService)" as P2 <<process>>
rectangle "P3\n调拨单处理\n(AllotOrderService)" as P3 <<process>>
rectangle "P4\n扫码调出处理\n(AllotOrderService)" as P4 <<process>>
rectangle "P5\n扫码调入处理\n(AllotOrderService)" as P5 <<process>>

' 数据存储
rectangle "D1\nstock_info\n(库存信息表)" as D1 <<storage>>
rectangle "D2\nstock_record\n(库存流水表)" as D2 <<storage>>
rectangle "D3\nstock_allot_order\n(调拨单主表)" as D3 <<storage>>
rectangle "D4\nstock_allot_detail\n(调拨明细表)" as D4 <<storage>>
rectangle "D5\nstock_mat_label\n(物料标签表)" as D5 <<storage>>

' 库存查询数据流
User --> Frontend : 库存查询条件
Frontend --> P1 : 查询请求
P1 --> D1 : 查询库存信息
D1 --> P1 : 库存数据
P1 --> Frontend : 库存列表/汇总
Frontend --> User : 库存展示

' 流水查询数据流
User --> Frontend : 流水查询条件
Frontend --> P2 : 查询请求
P2 --> D2 : 查询库存流水
D2 --> P2 : 流水数据
P2 --> Frontend : 流水列表
Frontend --> User : 流水展示

' 调拨单创建数据流
User --> Frontend : 调拨单数据
Frontend --> P3 : 创建请求
P3 --> D3 : 插入调拨单
P3 --> D4 : 插入调拨明细
P3 --> Frontend : 创建结果

' 扫码调出数据流
Scanner --> Frontend : 标签二维码(调出)
Frontend --> P4 : 扫码调出请求
P4 --> D3 : 查询调拨单
D3 --> P4 : 调拨单数据
P4 --> D4 : 查询调拨明细
D4 --> P4 : 明细数据

P4 --> D1 : 扣减调出仓库库存
P4 --> D2 : 插入调拨出库流水
P4 --> D4 : 更新已调出数量
P4 --> D3 : 更新调拨进度

P4 --> Frontend : 调出结果
Frontend --> User : 界面展示

' 扫码调入数据流
Scanner --> Frontend : 标签二维码(调入)
Frontend --> P5 : 扫码调入请求
P5 --> D3 : 查询调拨单
D3 --> P5 : 调拨单数据
P5 --> D4 : 查询调拨明细
D4 --> P5 : 明细数据

P5 --> D1 : 增加/新增调入仓库库存
P5 --> D2 : 插入调拨入库流水
P5 --> D4 : 更新已调入数量
P5 --> D3 : 更新状态为已完成

P5 --> Frontend : 调入结果
Frontend --> User : 界面展示

note right of P1
  多维度查询库存
  汇总统计
  安全库存预警
end note

note right of P2
  按时间范围查询
  流水类型过滤
  倒序排列
end note

note right of P3
  创建调拨单
  设置调拨状态
  初始化明细数量
end note

note right of P4
  扫码匹配明细
  扣减调出仓库库存
  @Transactional事务
end note

note right of P5
  增加调入仓库库存
  完成调拨流程
  @Transactional事务
end note

@enduml
```

管理员或仓管员通过前端页面发起库存查询请求，请求数据流入P1库存查询处理过程，P1从stock_info表查询库存数据，支持按物料、仓库、货位等多维度查询和汇总统计，查询结果返回前端展示，库存低于安全库存阈值时系统标红预警。管理员发起流水查询请求，请求数据流入P2流水查询处理过程，P2从stock_record表查询库存流水数据，按时间范围和流水类型过滤，按创建时间倒序排列返回流水列表。仓管员创建调拨单，调拨单数据流入P3调拨单处理过程，P3生成调拨单号，设置调拨状态为"待调拨"、调拨进度为"未调出"，将调拨单数据写入stock_allot_order表和stock_allot_detail表。仓管员扫描调拨单二维码进入扫码调出界面，依次扫描物料标签，扫码数据流入P4扫码调出处理过程，P4开启数据库事务，从stock_allot_order表和stock_allot_detail表查询调拨信息，扣减stock_info表中调出仓库的库存数量，向stock_record表插入调拨出库流水记录，更新stock_allot_detail表的已调出数量，更新stock_allot_order表的调拨进度为"已调出"。仓管员再次扫描调拨单二维码进入扫码调入界面，依次扫描物料标签，扫码数据流入P5扫码调入处理过程，P5开启数据库事务，增加或新增stock_info表中调入仓库的库存数量，向stock_record表插入调拨入库流水记录，更新stock_allot_detail表的已调入数量，更新stock_allot_order表的调拨进度为"已调入"、调拨状态为"已完成"，提交事务返回处理结果。整个数据流清晰地展示了库存管理与调拨业务从库存查询、流水追溯到跨仓库物料转移的完整流转过程。
