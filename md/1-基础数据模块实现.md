# 5.1 基础数据模块实现（物料、分类、BOM、仓库货位）

## 5.1.1 流程图

管理员登录系统后进入基础数据管理模块，首先选择需要管理的数据类型（物料、分类、BOM、仓库或货位），系统加载对应的数据列表。当执行新增操作时，管理员填写完整的基础数据信息，系统进行前端表单验证，验证通过后提交至后端。后端接收请求后执行权限校验，校验通过则进行数据完整性验证，验证成功后将数据插入数据库，修改数据状态为已添加，最后返回操作结果。若验证失败，则提示错误信息，管理员需重新填写数据。对于修改操作，系统首先查询原有数据并回显至表单，管理员修改后提交，系统更新数据库记录。对于删除操作，系统先检查是否存在关联数据，若存在则禁止删除并提示，若不存在则执行删除操作。流程结束后，系统刷新数据列表显示最新结果。

```plantuml
@startuml
title 基础数据管理业务流程

start
:管理员登录系统;
:进入基础数据管理模块;
:选择数据类型;

if (操作类型?) then (新增)
  :填写基础数据信息;
  :前端表单验证;
  if (验证通过?) then (是)
    :提交至后端;
    :权限校验;
    :数据完整性验证;
    if (验证成功?) then (是)
      :插入数据库;
      :修改状态为已添加;
      :返回成功响应;
      :刷新数据列表;
    else (否)
      :返回错误信息;
      :重新填写数据;
    endif
  else (否)
    :显示错误提示;
    :返回表单页面;
  endif
elseif (修改) then
  :查询原有数据;
  :回显至表单;
  :修改数据信息;
  :提交更新请求;
  :更新数据库记录;
  :返回成功响应;
  :刷新数据列表;
elseif (删除) then
  :选择待删除记录;
  :检查关联数据;
  if (存在关联?) then (是)
    :禁止删除;
    :提示关联信息;
    stop
  else (否)
    :执行删除操作;
    :删除数据库记录;
    :返回成功响应;
    :刷新数据列表;
  endif
else (查询)
  :输入查询条件;
  :执行查询操作;
  :返回数据列表;
  :分页显示结果;
endif

:操作完成;
stop

@enduml
```

## 5.1.2 顺序图

```plantuml
@startuml
title 物料信息新增顺序图

actor 管理员
participant "前端页面" as Frontend
participant "MatController" as Controller
participant "MatServiceImpl" as Service
participant "MatMapper" as Mapper
database "MySQL数据库" as DB

管理员 -> Frontend: 点击新增物料按钮
Frontend -> Frontend: 弹出表单对话框
管理员 -> Frontend: 填写物料信息
Frontend -> Frontend: 执行表单验证

Frontend -> Controller: POST /base/mat/add
Controller -> Controller: @PreAuthorize权限校验
Controller -> Controller: 设置创建人信息

Controller -> Service: insertBaseMat(baseMat)
Service -> Service: 设置创建时间
Service -> Service: 数据完整性验证

Service -> Mapper: insertBaseMat(baseMat)
Mapper -> DB: INSERT INTO base_mat
DB --> Mapper: 返回影响行数
Mapper --> Service: 返回插入结果

Service --> Controller: 返回操作结果
Controller -> Controller: 封装响应对象
Controller --> Frontend: 返回成功响应

Frontend -> Frontend: 关闭表单对话框
Frontend -> Frontend: 刷新物料列表
Frontend --> 管理员: 显示操作成功提示

@enduml
```

管理员点击新增物料按钮，前端页面弹出表单对话框。管理员填写物料编码、名称、规格型号、单位、分类、安全库存等信息后，前端执行表单验证。验证通过后，前端向MatController发送POST请求，携带物料信息数据。Controller首先执行权限校验，确认用户具有物料新增权限，然后设置创建人信息为当前登录用户。随后Controller调用MatServiceImpl的insertBaseMat方法，Service层设置创建时间并进行数据完整性验证，验证通过后调用MatMapper的insertBaseMat方法。Mapper层执行SQL插入操作，将物料数据写入MySQL数据库的base_mat表，数据库返回影响行数。Mapper将结果返回给Service，Service返回给Controller，Controller封装成统一的响应对象返回给前端。前端接收到成功响应后，关闭表单对话框，刷新物料列表，并向管理员显示操作成功提示。

## 5.1.3 程序流程图

```plantuml
@startuml
title 基础数据管理程序流程

start

:接收请求;

if (请求类型?) then (查询列表)
  :接收分页参数;
  :接收查询条件;
  :调用service.selectList方法;
  :执行分页查询;
  :返回数据列表;
  
elseif (新增数据) then
  :接收数据对象;
  :设置创建人信息;
  :设置创建时间;
  :调用service.insert方法;
  if (插入成功?) then (是)
    :返回成功响应;
  else (否)
    :返回失败响应;
  endif
  
elseif (修改数据) then
  :接收数据对象;
  :根据ID查询原数据;
  if (数据存在?) then (是)
    :设置更新人信息;
    :设置更新时间;
    :调用service.update方法;
    if (更新成功?) then (是)
      :返回成功响应;
    else (否)
      :返回失败响应;
    endif
  else (否)
    :返回数据不存在错误;
  endif
  
elseif (删除数据) then
  :接收数据ID数组;
  :检查关联数据;
  if (存在关联?) then (是)
    :返回禁止删除提示;
  else (否)
    :调用service.delete方法;
    :删除数据记录;
    if (删除成功?) then (是)
      :返回成功响应;
    else (否)
      :返回失败响应;
    endif
  endif
  
else (查询详情)
  :接收数据ID;
  :调用service.selectById方法;
  :返回数据详情;
endif

:请求处理完成;
stop

@enduml
```

该程序流程图描述了基础数据管理模块的核心操作流程，流程从"开始"节点启动，涵盖查询列表、新增数据、修改数据、删除数据及查询详情五大功能。当执行查询列表操作时，系统接收分页参数current、size及查询条件，通过调用service.selectList方法完成分页查询后返回数据列表。当执行新增数据操作时，系统接收数据对象，设置创建人信息和创建时间，调用service.insert方法将数据插入数据库，根据插入结果返回成功或失败响应。当执行修改数据操作时，系统接收数据对象并根据ID查询原数据，若数据存在则设置更新人信息和更新时间，调用service.update方法更新数据库记录，根据更新结果返回相应响应；若数据不存在则返回错误信息。当执行删除数据操作时，系统接收数据ID数组，首先检查是否存在关联数据，若存在则返回禁止删除提示；若不存在则调用service.delete方法删除数据记录，根据删除结果返回相应响应。当执行查询详情操作时，系统接收数据ID，调用service.selectById方法返回数据详情。整个流程清晰呈现了模块对基础数据的全生命周期管理，各操作独立执行且均从"开始"启动，经对应业务处理后至"结束"节点，体现了功能的完整性和流程的规范性。

## 5.1.4 物料BOM管理实现

### 5.1.4.1 流程图

管理员进入物料BOM管理界面，首先选择父物料，系统查询该父物料的BOM列表，展示所有子物料及其用量信息。当执行新增BOM操作时，管理员点击新增按钮，弹出表单对话框，选择子物料并填写用量数量。系统执行BOM关系验证，检查是否存在循环依赖，即子物料的BOM中是否包含当前父物料，若存在循环依赖则提示错误并禁止新增，若不存在循环依赖则继续。系统检查该父子物料关系是否已存在，若已存在则提示重复并禁止新增，若不存在则将BOM关系数据插入base_mat_bom表，记录父物料编码、子物料编码、用量及创建信息，返回成功响应并刷新BOM列表。对于修改BOM操作，管理员选择待修改的BOM记录，系统回显原有数据至表单，管理员修改用量数量后提交，系统更新数据库记录并刷新列表。对于删除BOM操作，管理员选择待删除的BOM记录，系统弹出确认对话框，确认后执行删除操作，解除父子物料的组成关系，删除数据库记录并刷新列表。通过BOM管理功能，系统能够清晰展示产品的物料构成，支持多层级BOM结构，为生产领料提供准确的物料清单依据。

```plantuml
@startuml
title 物料BOM管理流程

start
:进入物料BOM管理;
:选择父物料;
:查询BOM列表;
:展示子物料及用量;

if (操作类型?) then (新增BOM)
  :点击新增BOM按钮;
  :弹出表单对话框;
  :选择子物料;
  :填写用量数量;
  
  :执行BOM关系验证;
  if (存在循环依赖?) then (是)
    :提示循环依赖错误;
    :禁止新增;
    stop
  else (否)
  endif
  
  :检查父子关系是否已存在;
  if (关系已存在?) then (是)
    :提示重复关系;
    :禁止新增;
    stop
  else (否)
    :插入BOM记录;
    :记录父物料编码;
    :记录子物料编码;
    :记录用量;
    :设置创建信息;
    :返回成功响应;
    :刷新BOM列表;
  endif
  
elseif (修改BOM) then
  :选择待修改BOM记录;
  :回显原有数据;
  :修改用量数量;
  :提交更新请求;
  :更新数据库记录;
  :返回成功响应;
  :刷新BOM列表;
  
elseif (删除BOM) then
  :选择待删除BOM记录;
  :弹出确认对话框;
  if (确认删除?) then (是)
    :执行删除操作;
    :解除父子物料关系;
    :删除数据库记录;
    :返回成功响应;
    :刷新BOM列表;
  else (否)
    stop
  endif
  
else (查询BOM)
  :输入父物料编码;
  :查询BOM列表;
  :关联子物料信息;
  :展示BOM结构;
endif

:操作完成;
stop

@enduml
```

### 5.1.4.2 顺序图

```plantuml
@startuml
title 物料BOM新增顺序图

actor 管理员
participant "前端页面" as Frontend
participant "BomController" as Controller
participant "BomServiceImpl" as Service
participant "BomMapper" as Mapper
participant "MatMapper" as MatMapper
database "MySQL数据库" as DB

管理员 -> Frontend: 进入BOM管理页面
Frontend -> Frontend: 选择父物料
Frontend -> Controller: GET /base/bom/list?parentMatCode=xxx
Controller -> Service: selectBomList(parentMatCode)
Service -> Mapper: selectBomList(parentMatCode)
Mapper -> DB: SELECT * FROM base_mat_bom\nWHERE parent_mat_code = ?
DB --> Mapper: 返回BOM列表
Mapper --> Service: 返回BOM数据
Service --> Controller: 返回BOM列表
Controller --> Frontend: 返回JSON响应
Frontend -> Frontend: 渲染BOM列表

管理员 -> Frontend: 点击新增BOM按钮
Frontend -> Frontend: 弹出表单对话框

管理员 -> Frontend: 选择子物料并填写用量
Frontend -> Frontend: 执行前端验证

Frontend -> Controller: POST /base/bom/add
Controller -> Service: insertBaseBom(bom)

Service -> Service: 检查循环依赖
Service -> Mapper: checkCircularDependency(parentCode, childCode)
Mapper -> DB: 递归查询子物料的BOM结构
DB --> Mapper: 返回查询结果

alt 存在循环依赖
  Service --> Controller: 返回循环依赖错误
  Controller --> Frontend: 返回错误响应
  Frontend --> 管理员: 显示错误提示
  
else 不存在循环依赖
  Service -> Mapper: checkBomExists(parentCode, childCode)
  Mapper -> DB: SELECT COUNT(*) FROM base_mat_bom\nWHERE parent_mat_code = ?\nAND child_mat_code = ?
  DB --> Mapper: 返回数量
  
  alt BOM关系已存在
    Service --> Controller: 返回重复关系错误
    Controller --> Frontend: 返回错误响应
    Frontend --> 管理员: 显示重复提示
    
  else BOM关系不存在
    Service -> Service: 设置创建人和创建时间
    Service -> Mapper: insertBaseBom(bom)
    Mapper -> DB: INSERT INTO base_mat_bom\nVALUES (...)
    DB --> Mapper: 返回插入结果
    Mapper --> Service: 返回操作结果
    Service --> Controller: 返回成功结果
    Controller --> Frontend: 返回成功响应
    Frontend -> Frontend: 关闭表单对话框
    Frontend -> Frontend: 刷新BOM列表
    Frontend --> 管理员: 显示操作成功提示
  end
end

@enduml
```

管理员进入物料BOM管理页面，前端页面加载并等待管理员选择父物料。管理员选择父物料后，前端向BomController发送GET请求（/base/bom/list?parentMatCode=xxx）获取该父物料的BOM列表。Controller调用Service层的selectBomList方法，Service调用Mapper的selectBomList方法查询数据库（SELECT * FROM base_mat_bom WHERE parent_mat_code = ?），数据库返回BOM列表数据，Mapper将数据返回给Service，Service返回给Controller，Controller将JSON响应返回给前端，前端渲染BOM列表展示子物料及用量信息。管理员点击新增BOM按钮，前端弹出表单对话框。管理员选择子物料并填写用量数量，前端执行表单验证。验证通过后，前端向Controller发送POST请求（/base/bom/add）携带BOM数据。Controller调用Service层的insertBaseBom方法，Service首先检查循环依赖，调用Mapper的checkCircularDependency方法递归查询子物料的BOM结构，判断子物料的BOM中是否包含当前父物料，数据库返回查询结果。若存在循环依赖，Service返回循环依赖错误给Controller，Controller返回错误响应给前端，前端向管理员显示错误提示。若不存在循环依赖，Service调用Mapper的checkBomExists方法检查该父子物料关系是否已存在（SELECT COUNT(*) FROM base_mat_bom WHERE parent_mat_code = ? AND child_mat_code = ?），数据库返回数量。若BOM关系已存在，Service返回重复关系错误，前端显示重复提示。若BOM关系不存在，Service设置创建人和创建时间，调用Mapper的insertBaseBom方法插入数据库（INSERT INTO base_mat_bom VALUES (...)），数据库返回插入结果，Mapper返回操作结果给Service，Service返回成功结果给Controller，Controller返回成功响应给前端，前端关闭表单对话框，刷新BOM列表，向管理员显示操作成功提示。

### 5.1.4.3 程序流程图

```plantuml
@startuml
title 物料BOM管理程序流程

start
:接收请求;

if (请求类型?) then (查询BOM列表)
  :接收父物料编码;
  :调用service.selectBomList方法;
  :执行SQL查询;
  note right
    SELECT bmb.*,
           bm.mat_name,
           bm.spec_model,
           bm.unit
    FROM base_mat_bom bmb
    LEFT JOIN base_mat bm
      ON bmb.child_mat_code = bm.mat_code
    WHERE bmb.parent_mat_code = ?
    ORDER BY bmb.create_time DESC
  end note
  :关联子物料信息;
  :返回BOM列表;
  
elseif (新增BOM) then
  :接收BOM数据;
  :获取父物料编码;
  :获取子物料编码;
  :获取用量;
  
  :检查循环依赖;
  :递归查询子物料的BOM;
  if (子物料BOM包含父物料?) then (是)
    :返回循环依赖错误;
    note right
      防止出现A->B->A的循环
    end note
    stop
  else (否)
  endif
  
  :检查BOM关系是否已存在;
  :执行COUNT查询;
  if (COUNT > 0?) then (是)
    :返回重复关系错误;
    stop
  else (否)
  endif
  
  :设置创建人和创建时间;
  :执行INSERT操作;
  note right
    INSERT INTO base_mat_bom
    (parent_mat_code, child_mat_code,
     quantity, create_by, create_time)
    VALUES (?, ?, ?, ?, ?)
  end note
  :返回成功响应;
  
elseif (修改BOM) then
  :接收BOM数据;
  :获取BOM ID;
  :查询原BOM记录;
  if (记录存在?) then (是)
    :获取新用量;
    :设置更新人和更新时间;
    :执行UPDATE操作;
    note right
      UPDATE base_mat_bom
      SET quantity = ?,
          update_by = ?,
          update_time = ?
      WHERE bom_id = ?
    end note
    :返回成功响应;
  else (否)
    :返回记录不存在错误;
  endif
  
elseif (删除BOM) then
  :接收BOM ID;
  :执行DELETE操作;
  note right
    DELETE FROM base_mat_bom
    WHERE bom_id = ?
  end note
  if (删除成功?) then (是)
    :返回成功响应;
  else (否)
    :返回删除失败错误;
  endif
  
else (BOM展开查询)
  :接收父物料编码;
  :接收展开层级参数;
  :初始化结果列表;
  
  :递归查询BOM结构;
  repeat
    :查询当前层级子物料;
    :计算累计用量;
    note right
      累计用量 = 
      父级用量 × 当前用量
    end note
    :添加到结果列表;
    
    if (子物料有BOM?) then (是)
      :继续递归查询下一层;
    else (否)
    endif
  repeat while (达到最大层级?) is (否)
  -> 是;
  
  :返回完整BOM树;
endif

:请求处理完成;
stop

@enduml
```

该程序流程图描述了物料BOM管理的核心操作流程。当执行查询BOM列表操作时，系统接收父物料编码，调用Service层的selectBomList方法，执行多表关联SQL查询，从base_mat_bom表查询BOM记录，左关联base_mat表获取子物料的名称、规格型号、单位等信息，WHERE条件筛选指定父物料的BOM记录，按创建时间倒序排列，关联子物料信息后返回BOM列表。当执行新增BOM操作时，系统接收BOM数据，获取父物料编码、子物料编码和用量。系统首先检查循环依赖，递归查询子物料的BOM结构，判断子物料的BOM中是否包含父物料，若包含则返回循环依赖错误并终止操作，防止出现A->B->A的循环依赖情况。若不存在循环依赖，系统检查该父子物料关系是否已存在，执行COUNT查询统计数量，若COUNT大于0则返回重复关系错误并终止操作。若关系不存在，系统设置创建人和创建时间，执行INSERT操作将BOM数据插入base_mat_bom表，返回成功响应。当执行修改BOM操作时，系统接收BOM数据，获取BOM ID，查询原BOM记录，若记录存在则获取新用量，设置更新人和更新时间，执行UPDATE操作更新用量字段，返回成功响应；若记录不存在则返回错误。当执行删除BOM操作时，系统接收BOM ID，执行DELETE操作从base_mat_bom表删除记录，根据删除结果返回相应响应。当执行BOM展开查询操作时，系统接收父物料编码和展开层级参数，初始化结果列表，递归查询BOM结构。系统查询当前层级的子物料，计算累计用量（累计用量 = 父级用量 × 当前用量），将子物料信息添加到结果列表。若子物料本身也有BOM，系统继续递归查询下一层，直到达到最大层级或子物料无BOM为止，最终返回完整的BOM树形结构，展示产品的多层级物料构成。

## 5.1.5 仓库货位管理实现

### 5.1.5.1 流程图

管理员进入仓库货位管理界面，可选择管理仓库或货位。在仓库管理中，管理员点击新增仓库按钮，填写仓库编码、名称、类型及负责人等基本信息，系统执行前端表单验证，验证通过后提交至后端。后端执行仓库编码唯一性校验，若编码重复则返回错误提示，若编码唯一则将仓库数据插入base_warehouse表，设置创建人和创建时间，返回成功响应并刷新仓库列表。对于修改仓库操作，系统查询原有数据并回显，管理员修改后提交，系统更新数据库记录。对于删除仓库操作，系统首先检查该仓库下是否存在货位或库存记录，若存在则禁止删除并提示，若不存在则执行删除操作。在货位管理中，管理员选择所属仓库后，系统展示该仓库下的所有货位列表。管理员点击新增货位按钮，填写货位编码、名称及所属仓库，系统执行货位编码唯一性校验，校验通过后将货位数据插入base_location表，返回成功响应。系统支持打印仓库和货位标签功能，管理员选择待打印的仓库或货位，系统生成包含二维码的标签，仓库二维码内容格式为"WAREHOUSE:仓库编码"，货位二维码内容格式为"LOCATION:货位编码:仓库编码"。系统调用PDFService服务，使用预定义的PDF模板，将仓库或货位信息及二维码填充至模板，生成可打印的PDF文件供用户下载。现场人员可通过扫描二维码快速识别仓库和货位信息，提高入库、出库等操作的效率和准确性。

```plantuml
@startuml
title 仓库货位管理流程

start

if (管理对象?) then (仓库管理)
  :进入仓库管理界面;
  
  if (操作类型?) then (新增仓库)
    :点击新增仓库按钮;
    :填写仓库信息;
    note right
      - 仓库编码
      - 仓库名称
      - 仓库类型
      - 负责人
    end note
    :前端表单验证;
    if (验证通过?) then (是)
      :提交至后端;
      :执行编码唯一性校验;
      if (编码重复?) then (是)
        :返回错误提示;
        stop
      else (否)
        :插入仓库数据;
        :设置创建信息;
        :返回成功响应;
        :刷新仓库列表;
      endif
    else (否)
      :显示验证错误;
      stop
    endif
    
  elseif (修改仓库) then
    :选择待修改仓库;
    :查询原有数据;
    :回显至表单;
    :修改仓库信息;
    :提交更新请求;
    :更新数据库记录;
    :返回成功响应;
    :刷新仓库列表;
    
  elseif (删除仓库) then
    :选择待删除仓库;
    :检查关联数据;
    if (存在货位或库存?) then (是)
      :禁止删除;
      :提示关联信息;
      stop
    else (否)
      :执行删除操作;
      :删除数据库记录;
      :返回成功响应;
      :刷新仓库列表;
    endif
    
  else (打印仓库标签)
    :选择待打印仓库;
    :生成仓库二维码;
    note right
      二维码内容:
      WAREHOUSE:仓库编码
    end note
    :调用PDFService;
    :填充PDF模板;
    :生成PDF文件;
    :下载标签文件;
  endif
  
else (货位管理)
  :进入货位管理界面;
  :选择所属仓库;
  :展示货位列表;
  
  if (操作类型?) then (新增货位)
    :点击新增货位按钮;
    :填写货位信息;
    note right
      - 货位编码
      - 货位名称
      - 所属仓库
    end note
    :前端表单验证;
    if (验证通过?) then (是)
      :提交至后端;
      :执行编码唯一性校验;
      if (编码重复?) then (是)
        :返回错误提示;
        stop
      else (否)
        :插入货位数据;
        :设置创建信息;
        :返回成功响应;
        :刷新货位列表;
      endif
    else (否)
      :显示验证错误;
      stop
    endif
    
  elseif (修改货位) then
    :选择待修改货位;
    :查询原有数据;
    :回显至表单;
    :修改货位信息;
    :提交更新请求;
    :更新数据库记录;
    :返回成功响应;
    :刷新货位列表;
    
  elseif (删除货位) then
    :选择待删除货位;
    :检查关联数据;
    if (存在库存记录?) then (是)
      :禁止删除;
      :提示关联信息;
      stop
    else (否)
      :执行删除操作;
      :删除数据库记录;
      :返回成功响应;
      :刷新货位列表;
    endif
    
  else (打印货位标签)
    :选择待打印货位;
    :生成货位二维码;
    note right
      二维码内容:
      LOCATION:货位编码:仓库编码
    end note
    :调用PDFService;
    :填充PDF模板;
    :生成PDF文件;
    :下载标签文件;
  endif
endif

:操作完成;
stop

@enduml
```

### 5.1.5.2 顺序图

```plantuml
@startuml
title 货位标签打印顺序图

actor 管理员
participant "前端页面" as Frontend
participant "LocationController" as Controller
participant "LocationServiceImpl" as Service
participant "PDFService" as PDFService
participant "QRCodeUtil" as QRCode
database "MySQL数据库" as DB

管理员 -> Frontend: 进入货位管理页面
Frontend -> Controller: GET /base/location/list?warehouseCode=xxx
Controller -> Service: selectLocationList(warehouseCode)
Service -> DB: SELECT * FROM base_location\nWHERE warehouse_code = ?
DB --> Service: 返回货位列表
Service --> Controller: 返回货位数据
Controller --> Frontend: 返回JSON响应
Frontend -> Frontend: 渲染货位列表

管理员 -> Frontend: 选择货位并点击打印标签
Frontend -> Frontend: 获取货位信息

Frontend -> Controller: POST /base/location/printLabel
Controller -> Service: printLocationLabel(locationId)

Service -> DB: SELECT * FROM base_location\nWHERE location_id = ?
DB --> Service: 返回货位详情
Service -> DB: SELECT * FROM base_warehouse\nWHERE warehouse_code = ?
DB --> Service: 返回仓库详情

Service -> Service: 构建二维码内容
note right
  格式: LOCATION:货位编码:仓库编码
  示例: LOCATION:A01-001:WH001
end note

Service -> QRCode: generateQRCode(content, width, height)
QRCode -> QRCode: 创建QRCode对象
QRCode -> QRCode: 设置纠错级别
QRCode -> QRCode: 编码二维码内容
QRCode -> QRCode: 生成二维码图片
QRCode --> Service: 返回二维码图片Base64

Service -> PDFService: generateLocationLabelPDF(location, qrCode)
PDFService -> PDFService: 加载PDF模板
PDFService -> PDFService: 填充货位编码
PDFService -> PDFService: 填充货位名称
PDFService -> PDFService: 填充仓库名称
PDFService -> PDFService: 插入二维码图片
PDFService -> PDFService: 设置页面样式
PDFService -> PDFService: 生成PDF字节流
PDFService --> Service: 返回PDF字节数组

Service --> Controller: 返回PDF文件流
Controller -> Controller: 设置响应头
note right
  Content-Type: application/pdf
  Content-Disposition: attachment
end note
Controller --> Frontend: 返回PDF文件

Frontend -> Frontend: 触发浏览器下载
Frontend --> 管理员: 下载标签PDF文件

管理员 -> 管理员: 打印标签并粘贴至货位

@enduml
```

管理员进入货位管理页面，前端向LocationController发送GET请求（/base/location/list?warehouseCode=xxx）获取指定仓库的货位列表。Controller调用Service层的selectLocationList方法，Service查询数据库（SELECT * FROM base_location WHERE warehouse_code = ?），数据库返回货位列表，Service返回给Controller，Controller将JSON响应返回给前端，前端渲染货位列表展示。管理员选择需要打印标签的货位并点击打印标签按钮，前端获取货位信息并向Controller发送POST请求（/base/location/printLabel）。Controller调用Service层的printLocationLabel方法，Service首先查询货位详情（SELECT * FROM base_location WHERE location_id = ?），数据库返回货位详情；然后查询货位所属仓库的详情（SELECT * FROM base_warehouse WHERE warehouse_code = ?），数据库返回仓库详情。Service构建二维码内容，格式为"LOCATION:货位编码:仓库编码"，例如"LOCATION:A01-001:WH001"。Service调用QRCodeUtil工具类的generateQRCode方法生成二维码，QRCodeUtil创建QRCode对象，设置纠错级别，编码二维码内容，生成二维码图片，将二维码图片转换为Base64编码返回给Service。Service调用PDFService的generateLocationLabelPDF方法生成PDF标签，PDFService加载预定义的PDF模板，依次填充货位编码、货位名称、仓库名称等文本信息，插入二维码图片，设置页面样式（如字体、字号、对齐方式等），生成PDF字节流返回给Service。Service将PDF文件流返回给Controller，Controller设置HTTP响应头（Content-Type: application/pdf, Content-Disposition: attachment），将PDF文件返回给前端。前端触发浏览器下载，管理员获取标签PDF文件，打印标签并粘贴至实际货位位置，完成货位标签的制作和部署。

### 5.1.5.3 程序流程图

```plantuml
@startuml
title 仓库货位管理程序流程

start
:接收请求;

if (请求类型?) then (查询仓库列表)
  :接收查询条件;
  :调用service.selectWarehouseList;
  :执行SQL查询;
  note right
    SELECT * FROM base_warehouse
    WHERE warehouse_name LIKE ?
    AND warehouse_type = ?
    ORDER BY create_time DESC
  end note
  :返回仓库列表;
  
elseif (新增仓库) then
  :接收仓库数据;
  :获取仓库编码;
  :执行编码唯一性校验;
  :执行COUNT查询;
  if (COUNT > 0?) then (是)
    :返回编码重复错误;
    stop
  else (否)
  endif
  :设置创建人和创建时间;
  :执行INSERT操作;
  note right
    INSERT INTO base_warehouse
    (warehouse_code, warehouse_name,
     warehouse_type, keeper,
     create_by, create_time)
    VALUES (?, ?, ?, ?, ?, ?)
  end note
  :返回成功响应;
  
elseif (删除仓库) then
  :接收仓库ID;
  :查询该仓库下的货位数量;
  if (货位数量 > 0?) then (是)
    :返回存在关联货位错误;
    stop
  else (否)
  endif
  :查询该仓库的库存记录数量;
  if (库存记录 > 0?) then (是)
    :返回存在库存记录错误;
    stop
  else (否)
  endif
  :执行DELETE操作;
  note right
    DELETE FROM base_warehouse
    WHERE warehouse_id = ?
  end note
  :返回成功响应;
  
elseif (查询货位列表) then
  :接收仓库编码;
  :调用service.selectLocationList;
  :执行SQL查询;
  note right
    SELECT bl.*,
           bw.warehouse_name
    FROM base_location bl
    LEFT JOIN base_warehouse bw
      ON bl.warehouse_code = bw.warehouse_code
    WHERE bl.warehouse_code = ?
    ORDER BY bl.location_code ASC
  end note
  :关联仓库信息;
  :返回货位列表;
  
elseif (新增货位) then
  :接收货位数据;
  :获取货位编码;
  :获取仓库编码;
  :执行编码唯一性校验;
  :执行COUNT查询;
  if (COUNT > 0?) then (是)
    :返回编码重复错误;
    stop
  else (否)
  endif
  :设置创建人和创建时间;
  :执行INSERT操作;
  note right
    INSERT INTO base_location
    (location_code, location_name,
     warehouse_code,
     create_by, create_time)
    VALUES (?, ?, ?, ?, ?)
  end note
  :返回成功响应;
  
elseif (删除货位) then
  :接收货位ID;
  :查询该货位的库存记录数量;
  if (库存记录 > 0?) then (是)
    :返回存在库存记录错误;
    stop
  else (否)
  endif
  :执行DELETE操作;
  note right
    DELETE FROM base_location
    WHERE location_id = ?
  end note
  :返回成功响应;
  
elseif (打印仓库标签) then
  :接收仓库ID;
  :查询仓库详情;
  :构建二维码内容;
  note right
    格式: WAREHOUSE:仓库编码
  end note
  :生成二维码图片;
  :加载仓库标签PDF模板;
  :填充仓库编码;
  :填充仓库名称;
  :填充仓库类型;
  :插入二维码图片;
  :生成PDF文件;
  :返回PDF文件流;
  
else (打印货位标签)
  :接收货位ID;
  :查询货位详情;
  :查询仓库详情;
  :构建二维码内容;
  note right
    格式: LOCATION:货位编码:仓库编码
  end note
  :生成二维码图片;
  :加载货位标签PDF模板;
  :填充货位编码;
  :填充货位名称;
  :填充仓库名称;
  :插入二维码图片;
  :生成PDF文件;
  :返回PDF文件流;
endif

:请求处理完成;
stop

@enduml
```

该程序流程图描述了仓库货位管理的核心操作流程。当执行查询仓库列表操作时，系统接收查询条件，调用Service层的selectWarehouseList方法，执行SQL查询（SELECT * FROM base_warehouse WHERE warehouse_name LIKE ? AND warehouse_type = ? ORDER BY create_time DESC），返回仓库列表数据。当执行新增仓库操作时，系统接收仓库数据，获取仓库编码，执行编码唯一性校验，通过COUNT查询统计该编码的仓库数量，若COUNT大于0则返回编码重复错误并终止操作。若编码唯一，系统设置创建人和创建时间，执行INSERT操作将仓库数据插入base_warehouse表，返回成功响应。当执行删除仓库操作时，系统接收仓库ID，首先查询该仓库下的货位数量，若货位数量大于0则返回存在关联货位错误并终止操作；然后查询该仓库的库存记录数量，若库存记录大于0则返回存在库存记录错误并终止操作。若无关联数据，系统执行DELETE操作从base_warehouse表删除仓库记录，返回成功响应。当执行查询货位列表操作时，系统接收仓库编码，调用Service层的selectLocationList方法，执行多表关联SQL查询，从base_location表查询货位记录，左关联base_warehouse表获取仓库名称，WHERE条件筛选指定仓库的货位，按货位编码升序排列，关联仓库信息后返回货位列表。当执行新增货位操作时，系统接收货位数据，获取货位编码和仓库编码，执行编码唯一性校验，通过COUNT查询统计该编码的货位数量，若编码重复则返回错误并终止操作。若编码唯一，系统设置创建人和创建时间，执行INSERT操作将货位数据插入base_location表，返回成功响应。当执行删除货位操作时，系统接收货位ID，查询该货位的库存记录数量，若库存记录大于0则返回存在库存记录错误并终止操作，若无库存记录则执行DELETE操作删除货位记录，返回成功响应。当执行打印仓库标签操作时，系统接收仓库ID，查询仓库详情，构建二维码内容（格式为"WAREHOUSE:仓库编码"），生成二维码图片，加载仓库标签PDF模板，依次填充仓库编码、仓库名称、仓库类型等文本信息，插入二维码图片，生成PDF文件并返回PDF文件流。当执行打印货位标签操作时，系统接收货位ID，查询货位详情和仓库详情，构建二维码内容（格式为"LOCATION:货位编码:仓库编码"），生成二维码图片，加载货位标签PDF模板，填充货位编码、货位名称、仓库名称等信息，插入二维码图片，生成PDF文件并返回PDF文件流供用户下载打印。仓库货位管理的规范化实现为后续库存管理提供了清晰的空间定位基础，标签打印功能通过二维码技术实现了仓库货位的快速识别和定位。

